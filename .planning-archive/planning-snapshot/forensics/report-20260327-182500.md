# Forensic Report

**Generated:** 2026-03-27T18:25:00+0100
**Problem:** Checkpoint MARC-04 non funzionano (`window._m.arcPercent = N` non ha effetto), si sente solo CH1, test VISL-02 produce risultati inattesi (kick con melody).

---

## Evidence Summary

### Git Activity

**Outer repo** (`.planning/` artifacts):
- **Last commit:** 2026-03-27 17:10 — `docs(04-02): complete V3 gate director.js plan`
- **Commits (last 30):** 30, tutti `docs(*)` — nessun source commit
- **Uncommitted changes:** Solo file untracked (`MACH:INE II/`, `.claude/`, etc.)
- **Active worktrees:** 3 orfani oltre al main

**Inner repo** (`MACH:INE II/.git` — source code):
- **Last commit:** `1a4c535` — `fix(debug): replace window._m.arcPercent with arc(v) helper`
- **Commits rilevanti Phase 04:** `9815084`, `a6bd6f4`, `c6a21db`, `995a48d`, `134810b`, `d109a6a`, `1a4c535`
- **Status:** pulito, nessuna modifica staged/unstaged

### Planning State

- **Milestone:** v1.0
- **Fase corrente:** 04-integrazione-e-visual-system — EXECUTING
- **STATE.md stopped_at:** `Completed 04-01-PLAN.md — CFG.VISUAL config block` ← **STALE**
- **Piani completati (reale):** 04-01 ✅, 04-02 ✅, 04-03 in corso
- **Blockers attivi:** nessuno formale, ma VISL-02 strutturalmente irraggiungibile (vedi Anomalie)

### Artifact Completeness

| Phase | PLAN | CONTEXT | RESEARCH | SUMMARY | VERIFICATION |
|-------|------|---------|----------|---------|-------------|
| 00-infrastruttura | ✅ | ❌ | ✅ | ✅ (x4) | ✅ |
| 01-macrocomposer  | ✅ | ✅ | ✅ | ✅ (x2) | ✅ |
| 02-rhythmlayer    | ✅ | ✅ | ❌ | ✅ (x3) | ✅ |
| 03-melodylayer    | ✅ | ✅ | ✅ | ✅ (x3) | ✅ |
| 04-integrazione   | ✅ (x3) | ✅ | ✅ | ✅ (x2) | ❌ (attesa 04-03) |

---

## Anomalies Detected

### 1. Debug API errata nel piano 04-03 — Confidence: HIGH

**Evidence:** `04-03-PLAN.md` righe 153, 161, 170, 179, 187, 195–200 usano `window._m.arcPercent = N` e `window._m.rhythmicDensity = N` come procedure di test.

**Root cause tecnica:** `updateMacroComposer(dt)` è chiamata ogni ~2ms dal MIDI Worker e sovrascrive `macroState.arcPercent` ad ogni tick. Modificare direttamente `window._m.arcPercent` ha effetto per meno di un tick prima di essere azzerato. La proprietà corretta è `macroState._debugArc` (controllata in Step A di `updateMacroComposer`), accessibile via `window.arc(N)`.

**Conferma:** commit `1a4c535` nel inner repo recita esplicitamente `fix(debug): replace window._m.arcPercent with arc(v) helper` — la correzione fu committata ma il PLAN.md non fu aggiornato.

**Impatto:** L'utente non riesce a testare nessuno dei 5 checkpoint. Il gate blocking `04-03-02` non può essere superato con la procedura documentata.

---

### 2. VISL-02 "melody dominant" strutturalmente irraggiungibile — Confidence: HIGH

**Evidence:** I checkpoint in `CFG.MACRO.checkpoints` definiscono i valori massimi per ogni dimensione:

| pct   | rD  | hC  | mA  | tD  | `Math.max(mA,tD)` | Dominante |
|-------|-----|-----|-----|-----|-------------------|-----------|
| 0.44  | 0.3 | 0.7 | 0.4 | 0.4 | 0.4               | harmony   |
| 0.62  | 0.5 | 1.0 | 0.5 | 0.5 | 0.5               | harmony   |
| 0.73  | 0.7 | 0.7 | 0.6 | 0.6 | 0.6               | tie/isteresi |
| 0.80  | 0.9 | 0.6 | 0.7 | 0.7 | 0.7               | rhythm    |
| 0.84  | 1.0 | 0.6 | 0.8 | 0.7 | 0.8               | rhythm    |
| 0.89  | 0.5 | 0.4 | 0.5 | 0.5 | 0.5               | tie/isteresi |

`melodicActivity` e `textureDepth` non superano MAI simultaneamente sia `rhythmicDensity` che `harmonicColor`. `_getDominantLayer()` quindi non ritorna mai `'melody'` durante il normale arco del concerto.

**Impatto:** La palette `magenta` (firma melody) non sarà mai visibile in produzione. Il test VISL-02 che chiede di verificare la firma magenta non può passare via `window.arc(N)`.

---

### 3. Orphaned worktrees — Confidence: MEDIUM

**Evidence:**
```
.claude/worktrees/agent-a060dbcd  → commit 5a820ba  (merged via c723d39)
.claude/worktrees/agent-a9b0c2f9  → commit 9c0915a  (merged via 03726b0)
.claude/worktrees/agent-a9f6bb85  → commit 0c7b8fb  (Phase 01, mai merged al main del outer repo)
```

I primi due sono stati mergiati nel main del outer repo ma i worktree non sono stati rimossi. Il terzo (`agent-a9f6bb85`) punta a un commit Phase 01 — probabilmente un agente che aveva un branch della documentazione.

**Impatto:** Nessun impatto funzionale immediato. Spreco disco, rischio confusione se vengono rilanciate operazioni su quei branch.

---

### 4. STATE.md stale — Confidence: MEDIUM

**Evidence:** `STATE.md` riporta `stopped_at: Completed 04-01-PLAN.md` con `last_updated: 2026-03-27T15:58:44`. Nella realtà, anche 04-02 è stato completato (commit `94aa3ee` alle 17:10) e 04-03 è in esecuzione.

**Impatto:** I tool GSD potrebbero rieseguire 04-02 credendolo incompleto. `progress: 13/15 piani` non è aggiornato.

---

### 5. Dual-repository architecture — Confidence: LOW (by design, ma non documentata)

**Evidence:** Due repository git separati:
- Outer: `.planning/`, `CLAUDE.md`, `STATE.md` ecc.
- Inner: `MACH:INE II/.git` — source code, tutti i `feat(*)` e `fix(*)` commits

Il GSD workflow (`execute-phase`, `forensics`) gira nel outer repo. Le verifiche `git log --all -- "MACH:INE II/src/..."` nel outer repo ritornano sempre vuote, rendendo difficile tracciare le source changes via GSD tools.

**Impatto:** I SUMMARY.md referenziano commit della inner repo (es. `a6bd6f4`) che appaiono inesistenti se si ispeziona dall'outer repo. Può causare falsi positivi in futuri forensics.

---

## Root Cause Hypothesis

**Causa principale:** Il piano `04-03-PLAN.md` è stato scritto con procedure di debug obsolete o errate. La modifica diretta di `window._m.arcPercent` non funziona perché `updateMacroComposer()` sovrascrive quel valore ogni 2ms — il corretto override è via `window.arc(N)` che imposta `_debugArc`. Questa incompatibilità è già stata riconosciuta (commit `1a4c535`: "replace window._m.arcPercent with arc(v) helper") ma il piano non è stato aggiornato. Il secondo problema strutturale (VISL-02 melody mai dominante) è un gap di design nei checkpoint: `melodicActivity` è sistematicamente più bassa di `harmonicColor` o `rhythmicDensity` in ogni punto dell'arco.

---

## Recommended Actions

1. **Usa `window.arc(N)` per i 5 checkpoint** (non `window._m.arcPercent = N`):
   ```js
   window.arc(0.0)   // Atto I — attendi 15s
   window.arc(0.25)  // Atto II
   window.arc(0.50)  // Atto III — harmony domina, cold palette
   window.arc(0.75)  // Atto IV
   window.arc(0.90)  // Atto V — dissoluzione
   window.arc()      // rilascia il freeze
   ```

2. **VISL-02 melody: accetta come known gap o correggi la curva**
   - Opzione A: accettare che la firma magenta non emerga nel concerto normale — la palette melody è una firma "di riserva"
   - Opzione B: alzare `mA`/`tD` nei checkpoint intermedi (es. pct=0.73: mA→0.8, tD→0.8) sopra sia rD che hC

3. **Solo CH1 è comportamento corretto** a arcPercent=0 — il drone CH2 parte dopo bar 2 (~5s), poi aumenta con `window.arc(0.4+)`

4. **Pulizia worktree:**
   ```bash
   # nel outer repo
   git worktree remove .claude/worktrees/agent-a060dbcd
   git worktree remove .claude/worktrees/agent-a9b0c2f9
   git worktree remove .claude/worktrees/agent-a9f6bb85
   ```

5. **Aggiorna STATE.md** per riflettere il completamento di 04-02 e lo stato corrente di 04-03.

---

*Report generato da `/gsd:forensics`. Percorsi assoluti redatti.*
