---
phase: 02-rhythmlayer-e-midi-output
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - "MACH:INE II/src/main.js"
autonomous: true
requirements: [RITM-02, MIDI-04]

must_haves:
  truths:
    - "Con V3_MODE=true, updateRhythmLayer(dt) viene chiamato ogni tick del Worker accanto a updateMacroComposer e updateHarmonyLayer"
    - "initRhythmLayer() viene chiamato al boot quando V3_MODE e' true"
    - "Il codice v2 rimane intatto nel branch else — zero regressioni"
  artifacts:
    - path: "MACH:INE II/src/main.js"
      provides: "V3_MODE routing per RhythmLayer"
      contains: "updateRhythmLayer"
  key_links:
    - from: "MACH:INE II/src/main.js"
      to: "MACH:INE II/src/rhythm-layer.js"
      via: "import { initRhythmLayer, updateRhythmLayer }"
      pattern: "updateRhythmLayer"
    - from: "MACH:INE II/src/main.js"
      to: "MACH:INE II/src/macro-composer.js"
      via: "updateMacroComposer(dt) chiamato prima di updateRhythmLayer(dt)"
      pattern: "updateMacroComposer.*updateRhythmLayer"
---

<objective>
Aggiungere il routing V3_MODE per RhythmLayer in main.js — import, init al boot e update nel midiWorker.onmessage.

Purpose: Collegare rhythm-layer.js al loop di esecuzione v3, completando il wiring della Phase 2.
Output: 3 modifiche chirurgiche a main.js (~5 righe aggiunte)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-rhythmlayer-e-midi-output/02-CONTEXT.md
@.planning/phases/01-macrocomposer-e-harmonylayer/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Stato corrente di main.js — le 3 zone da modificare -->

Zona 1 — imports v3 (riga ~27-28 attuale):
```javascript
// ── v3 layer system (Phase 1) ──
import { initMacroComposer, updateMacroComposer } from './macro-composer.js';
import { initHarmonyLayer, updateHarmonyLayer } from './harmony-layer.js';
```

Zona 2 — boot init v3 (riga ~144-148 attuale):
```javascript
  if (CFG.V3_MODE) {
    initMacroComposer();
    initHarmonyLayer();
    sendMIDIStart();
    console.log('[V3] MacroComposer + HarmonyLayer initialized');
  }
```

Zona 3 — midiWorker.onmessage v3 branch (riga ~245-256 attuale):
```javascript
    if (CFG.V3_MODE) {
      // ── v3 layer system: MacroComposer + HarmonyLayer ──
      // I composer v2 non vengono chiamati (D-08: rimangono intatti come reference/fallback)
      updateMacroComposer(dt);
      updateHarmonyLayer(dt);

      // MIDI clock: usa bpmReference dal MacroComposer config
      const bpm = CFG.MACRO.bpmReference;
      ...
```

From MACH:INE II/src/rhythm-layer.js (creato in Plan 02-02):
```javascript
export function initRhythmLayer() { ... }
export function updateRhythmLayer(dt) { ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: V3_MODE routing per RhythmLayer in main.js</name>
  <files>MACH:INE II/src/main.js</files>
  <read_first>
    - MACH:INE II/src/main.js (INTERO FILE — 309 righe. Localizzare le 3 zone esatte: import v3 ~riga 27, boot init ~riga 144, midiWorker.onmessage ~riga 245)
    - MACH:INE II/src/rhythm-layer.js (verificare che gli export initRhythmLayer e updateRhythmLayer esistano)
  </read_first>
  <action>
Tre modifiche chirurgiche a main.js:

**Modifica 1 — Import (dopo riga 28, accanto agli import v3 esistenti):**
Aggiungere:
```javascript
import { initRhythmLayer, updateRhythmLayer } from './rhythm-layer.js';
```
Posizione: DOPO la riga `import { initHarmonyLayer, updateHarmonyLayer } from './harmony-layer.js';`

**Modifica 2 — Boot init (dentro il blocco `if (CFG.V3_MODE)` del boot, dopo `initHarmonyLayer()`):**
Aggiungere:
```javascript
    initRhythmLayer();
```
Posizione: DOPO `initHarmonyLayer();` e PRIMA di `sendMIDIStart();`
Aggiornare il log message a: `console.log('[V3] MacroComposer + HarmonyLayer + RhythmLayer initialized');`

**Modifica 3 — Worker update (dentro il blocco `if (CFG.V3_MODE)` del midiWorker.onmessage, dopo `updateHarmonyLayer(dt)`):**
Aggiungere:
```javascript
      updateRhythmLayer(dt);
```
Posizione: DOPO `updateHarmonyLayer(dt);` e PRIMA del commento `// MIDI clock: usa bpmReference`
Aggiornare il commento di sezione a: `// ── v3 layer system: MacroComposer + HarmonyLayer + RhythmLayer ──`

**NON toccare:**
- Il branch `else` (v2 legacy) — rimane identico
- Nessun'altra riga di main.js
- Il MIDI clock block rimane invariato (usa gia' CFG.MACRO.bpmReference, condiviso da tutti i layer)
  </action>
  <acceptance_criteria>
    - [ ] grep "import { initRhythmLayer, updateRhythmLayer } from './rhythm-layer.js'" "MACH:INE II/src/main.js" restituisce match
    - [ ] grep "initRhythmLayer()" "MACH:INE II/src/main.js" restituisce match
    - [ ] grep "updateRhythmLayer(dt)" "MACH:INE II/src/main.js" restituisce match
    - [ ] grep "RhythmLayer initialized" "MACH:INE II/src/main.js" restituisce match (log aggiornato)
    - [ ] grep -c "updateMacroComposer" "MACH:INE II/src/main.js" restituisce 1 (non duplicato)
    - [ ] grep -c "updateHarmonyLayer" "MACH:INE II/src/main.js" restituisce 1 (non duplicato)
    - [ ] grep -c "updateRhythmLayer" "MACH:INE II/src/main.js" restituisce 1 (aggiunto una volta sola)
    - [ ] grep "updateComposer(dt)" "MACH:INE II/src/main.js" restituisce match (v2 branch intatto)
    - [ ] L'ordine nel Worker e': updateMacroComposer(dt) PRIMA di updateRhythmLayer(dt) — verificare con grep -A2 "updateMacroComposer" che mostra updateHarmonyLayer e poi updateRhythmLayer nelle righe successive
    - [ ] wc -l "MACH:INE II/src/main.js" restituisce circa 312-315 (3-5 righe aggiunte rispetto a 309 originali)
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/Edo_1/MACH-INE II" && grep -c "updateRhythmLayer" "MACH:INE II/src/main.js" && grep -c "initRhythmLayer" "MACH:INE II/src/main.js" && grep "import.*rhythm-layer" "MACH:INE II/src/main.js" && grep -A3 "updateMacroComposer" "MACH:INE II/src/main.js" | head -5 && echo "PASS: main.js routing verified"</automated>
  </verify>
  <done>main.js ha il routing V3_MODE completo: import, init al boot e updateRhythmLayer(dt) nel Worker loop — l'ordine di chiamata e' MacroComposer → HarmonyLayer → RhythmLayer. Il branch v2 e' intatto.</done>
</task>

</tasks>

<verification>
```bash
cd "/Users/Edo_1/MACH-INE II"

# Import presente
grep "rhythm-layer" "MACH:INE II/src/main.js"

# Init nel boot V3
grep -A5 "V3_MODE" "MACH:INE II/src/main.js" | grep "initRhythmLayer"

# Update nel Worker
grep -A5 "updateHarmonyLayer" "MACH:INE II/src/main.js" | grep "updateRhythmLayer"

# V2 branch intatto
grep "updateComposer7" "MACH:INE II/src/main.js"

# Ordine di esecuzione corretto
grep -n "updateMacroComposer\|updateHarmonyLayer\|updateRhythmLayer" "MACH:INE II/src/main.js"
```
</verification>

<success_criteria>
main.js contiene il routing V3_MODE per RhythmLayer: import, init e update. L'ordine nel Worker e' MacroComposer → HarmonyLayer → RhythmLayer (il MacroComposer aggiorna rhythmicDensity PRIMA che RhythmLayer lo legga). Il branch v2 legacy e' identico all'originale.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rhythmlayer-e-midi-output/02-03-SUMMARY.md`
</output>
