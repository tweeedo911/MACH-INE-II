---
phase: 03-melodylayer-e-texturelayer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "MACH:INE II/src/config.js"
autonomous: true
requirements: [MELO-01, MELO-02]

must_haves:
  truths:
    - "CFG.MELODY esiste in config.js con tutti i parametri per loop prime, seed, gating e velocity"
    - "I loop lengths 7/11/13 sono definiti come costanti in CFG.MELODY"
    - "I pitch range per CH3/CH5/CH6 rispettano MIDI-02"
  artifacts:
    - path: "MACH:INE II/src/config.js"
      provides: "CFG.MELODY config block"
      contains: "MELODY:"
  key_links:
    - from: "MACH:INE II/src/config.js"
      to: "MACH:INE II/src/melody-texture-layer.js"
      via: "import { CFG } from './config.js'"
      pattern: "CFG\\.MELODY"
---

<objective>
Aggiungere il blocco `CFG.MELODY` a config.js con tutti i parametri numerici per MelodyTextureLayer: lunghezze loop prime (7/11/13), seed motivico, gating, pitch range, velocity target, probabilita emissione e durate nota.

Purpose: Centralizzare tutti i parametri in config.js prima di implementare il modulo (config-first pattern stabilito in Phase 0/1/2).
Output: Sezione CFG.MELODY in config.js, zero magic numbers pronti per il modulo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-melodylayer-e-texturelayer/03-CONTEXT.md
@.planning/phases/03-melodylayer-e-texturelayer/03-RESEARCH.md

<interfaces>
<!-- CFG.RHYTHM pattern da replicare — struttura config gia stabilita -->
<!-- Da MACH:INE II/src/config.js linee 500-580: -->

```javascript
// Pattern struttura: sezione top-level in CFG con sotto-oggetti per parametri
RHYTHM: {
  bpmSource: 'macro',
  bpm: 88,
  phaseThresholds: { arhythmic: 0.0, emerging: 0.15, groove: 0.40, climax: 0.70, dissolving: 0.85 },
  kick: { note: 36, velDownbeat: 105, velOffbeat: 85, humanizeRange: 8, ... },
  hat: { pitchCluster: [36, 38, 40, 42], velFloor: 15, velTarget: {...}, ... },
  perc: { notes: {...}, additiveEntry: [...], ... },
  midi: { downbeatBoost: 0.08, offbeatReduce: 0.05, pitchRange: {...}, ... },
},
```

<!-- CFG.MACRO.modes e droneRoot — usati da melody-texture-layer.js per note pool -->
```javascript
MACRO: {
  concertDurationSec: 2700,
  bpmReference: 88,
  modes: {
    'A_lydian':    [45, 57, 59, 61, 63, 64, 66, 68, 69, 71, 73, 75, 76, 78, 80, 81],
    'Bb_phrygian': [46, 58, 59, 61, 63, 65, 66, 68, 70, 71, 73, 75, 77, 78, 80, 82],
    'D_dorian':    [38, 50, 52, 53, 55, 57, 59, 60, 62, 64, 65, 67, 69, 71, 72, 74],
    'C#_dorian':   [37, 49, 51, 52, 54, 56, 58, 59, 61, 63, 64, 66, 68, 70, 71, 73],
    'E_phrygian':  [40, 52, 53, 55, 57, 59, 60, 62, 64, 65, 67, 69, 71, 72, 74, 76],
  },
  droneRoot: {
    'A_lydian': 57, 'Bb_phrygian': 57, 'D_dorian': 50, 'C#_dorian': 57, 'E_phrygian': 57,
  },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungere CFG.MELODY config block a config.js</name>
  <files>MACH:INE II/src/config.js</files>
  <read_first>MACH:INE II/src/config.js</read_first>
  <action>
Aggiungere il blocco `MELODY:` come nuova sezione top-level di CFG, DOPO il blocco `RHYTHM:` (che termina intorno alla riga 600). Inserire un commento header in stile progetto:

```javascript
  // ── MelodyTextureLayer v3 — CH3 bass melodico, CH5 voce con memoria, CH6 voce indipendente ──
  MELODY: {
    // ── Loop lengths — numeri primi (MELO-02, D-10) ──
    // LCM(7,11,13) = 1001 step — poliritmo perpetuo, nessun ciclo identico
    loopLenCH3: 7,      // CH3 BASS — 7 step
    loopLenCH5: 11,     // CH5 VOICE — 11 step
    loopLenCH6: 13,     // CH6 LEAD — 13 step

    // ── Gating su melodicActivity ──
    activityGateFloor: 0.04,    // sotto questa soglia tutti i canali tacciono
    seedCaptureGateMin: 0.02,   // soglia piu bassa per permettere cattura seed in apertura

    // ── Pitch range per canale (MIDI-02) ──
    pitchRange: {
      3: { min: 24, max: 48 },   // CH3 BASS: C1=24 — C3=48
      5: { min: 36, max: 84 },   // CH5 VOICE: C2=36 — C6=84
      6: { min: 48, max: 84 },   // CH6 LEAD: C3=48 — C6=84 (ottava sopra CH5 base)
    },

    // ── Velocity target per fase melodica ──
    velTarget: {
      sparse:  { ch3: 55, ch5: 45, ch6: 35 },   // apertura / dissoluzione
      medium:  { ch3: 70, ch5: 60, ch6: 50 },   // sezione intermedia
      dense:   { ch3: 85, ch5: 75, ch6: 65 },   // climax
    },
    velHumanize: 8,   // deviazione gaussiana velocity (identico a RHYTHM)

    // ── Seed motivico (MELO-01, D-07, D-08, D-09) ──
    seedLength:    4,       // numero di note nel motivo seed
    seedWindowEnd: 0.15,    // arcPercent massimo per cattura (~6.75min su 45min)
    seedReturnAt:  0.75,    // arcPercent trigger ritorno (~min33.75 su 45min)
    seedReturnVel: 65,      // velocity del ritorno motivico su CH5
    seedReturnNoteSpacingBars: 0.5,  // spacing tra note seed return in bar

    // ── Probabilita emissione per step (D-06: default meno note, non piu) ──
    // Scalata automaticamente da melodicActivity
    emitProbability: {
      ch3Base: 0.45,   // basso suona piu spesso (struttura armonica)
      ch5Base: 0.25,   // voce melodica rarefatta
      ch6Base: 0.20,   // lead ancora piu rado
    },

    // ── Durata note per canale (ms) — dipende dalla fase ──
    noteDur: {
      ch3: { sparse: 1800, dense: 900 },   // basso lungo in apertura, staccato al climax
      ch5: { sparse: 1200, dense: 600 },
      ch6: { sparse: 800,  dense: 400 },
    },

    // ── Markov voice weights (pattern da composer.js nextVoiceNote) ──
    markov: {
      stepBonus:   2.5,    // bonus movimento per grado (intervallo <= 3 semitoni)
      jumpPenalty: 0.15,   // penalty salto grande (intervallo > 7 semitoni)
      seedAffinity:     1.8,    // bonus se intervallo corrisponde al seed
      seedNearAffinity: 1.3,    // bonus se intervallo vicino al seed (diff 1)
      ch6JumpPreference: 1.6,   // CH6 preferisce salti — carattere angoloso (D-03)
      ch6StepReduction:  0.7,   // CH6 riduce bonus step per differenziarsi da CH5
    },

    // ── Arpeggi incrociati (D-05) ──
    arpeggio: {
      activeRange: { min: 0.35, max: 0.70 },  // arcPercent range per arpeggi (~min15-31)
      noteSpacingMs: 350,    // spacing tra note arpeggio
      threshold:    0.5,     // melodicActivity minima per attivare cross-arpeggio
      delayStp:     2,       // steps CH6 arpeggiation delay rispetto a CH5
    },

    // ── MIDI optimization (pattern da RHYTHM) ──
    midi: {
      downbeatBoost:  0.06,    // +6% velocity su downbeat (MIDI-01, piu delicato della ritmica)
      offbeatReduce:  0.04,    // -4% velocity su offbeat
      noteOffsetMs:   { min: 3, max: 18 },   // sfasamento anti-meccanico (MIDI-03)
    },
  },
```

Il blocco deve essere inserito PRIMA della chiusura `};` del CFG object. Nessun altro valore di CFG deve essere modificato. Il commento header segue lo stile dei blocchi MACRO e RHYTHM.
  </action>
  <verify>
    <automated>grep -c "MELODY:" "MACH:INE II/src/config.js"</automated>
  </verify>
  <acceptance_criteria>
    - config.js contiene `MELODY:` come proprieta top-level di CFG
    - config.js contiene `loopLenCH3: 7`
    - config.js contiene `loopLenCH5: 11`
    - config.js contiene `loopLenCH6: 13`
    - config.js contiene `seedLength: 4`
    - config.js contiene `seedWindowEnd: 0.15`
    - config.js contiene `seedReturnAt: 0.75`
    - config.js contiene `activityGateFloor: 0.04`
    - config.js contiene `pitchRange:` con chiavi 3, 5, 6
    - config.js contiene `emitProbability:` con ch3Base, ch5Base, ch6Base
    - config.js contiene `markov:` con stepBonus, jumpPenalty, seedAffinity
    - config.js contiene `ch6JumpPreference` e `ch6StepReduction` (differenziazione D-03)
    - config.js contiene `arpeggio:` con activeRange, noteSpacingMs, threshold: 0.5, delayStp: 2 (D-05)
    - Nessun valore pre-esistente di CFG e stato modificato
    - Il file non supera 700 righe totali (618 attuali + ~70 nuove)
  </acceptance_criteria>
  <done>CFG.MELODY block presente in config.js con tutti i parametri per loop prime, seed motivico, gating, velocity, Markov weights, arpeggio e MIDI optimization — zero magic numbers pronti per melody-texture-layer.js</done>
</task>

</tasks>

<verification>
- `grep "MELODY:" "MACH:INE II/src/config.js"` ritorna 1 match
- `grep "loopLenCH3: 7" "MACH:INE II/src/config.js"` ritorna 1 match
- `grep "seedReturnAt" "MACH:INE II/src/config.js"` ritorna 1 match
- Il file config.js e parsabile come ES module (nessun errore di sintassi)
</verification>

<success_criteria>
CFG.MELODY esiste in config.js con tutti i parametri definiti nel RESEARCH.md — ogni valore numerico che melody-texture-layer.js consumera e centralizzato qui, nessun magic number nel codice del modulo.
</success_criteria>

<output>
After completion, create `.planning/phases/03-melodylayer-e-texturelayer/03-01-SUMMARY.md`
</output>
