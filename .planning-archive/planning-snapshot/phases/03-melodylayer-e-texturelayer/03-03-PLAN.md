---
phase: 03-melodylayer-e-texturelayer
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - "MACH:INE II/src/main.js"
autonomous: true
requirements: [MELO-01, MELO-02, MELO-03]

must_haves:
  truths:
    - "updateMelodyTextureLayer(dt) viene chiamato nel Worker loop V3_MODE dopo updateRhythmLayer(dt)"
    - "initMelodyTextureLayer() viene chiamato nel boot V3_MODE dopo initRhythmLayer()"
    - "Il branch v2 legacy rimane intatto e invariato"
  artifacts:
    - path: "MACH:INE II/src/main.js"
      provides: "V3_MODE routing per MelodyTextureLayer"
      contains: "updateMelodyTextureLayer"
  key_links:
    - from: "MACH:INE II/src/main.js"
      to: "MACH:INE II/src/melody-texture-layer.js"
      via: "import { initMelodyTextureLayer, updateMelodyTextureLayer } from './melody-texture-layer.js'"
      pattern: "import.*melody-texture-layer"
    - from: "MACH:INE II/src/main.js"
      to: "MACH:INE II/src/config.js"
      via: "CFG.V3_MODE check"
      pattern: "CFG\\.V3_MODE"
---

<objective>
Collegare melody-texture-layer.js al loop di esecuzione v3 in main.js: import, init al boot e updateMelodyTextureLayer(dt) nel midiWorker.onmessage. Tre modifiche chirurgiche identiche al pattern Phase 2 Plan 03.

Purpose: Attivare il MelodyTextureLayer nel sistema V3_MODE — completa il ciclo di esecuzione MacroComposer -> HarmonyLayer -> RhythmLayer -> MelodyTextureLayer.
Output: main.js aggiornato con routing MelodyTextureLayer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-melodylayer-e-texturelayer/03-CONTEXT.md
@.planning/phases/02-rhythmlayer-e-midi-output/02-03-SUMMARY.md

<interfaces>
<!-- main.js — integration points esatti (linee attuali) -->

<!-- Riga 26-29: blocco import v3 layer system -->
```javascript
// ── v3 layer system (Phase 1 + Phase 2) ──
import { initMacroComposer, updateMacroComposer } from './macro-composer.js';
import { initHarmonyLayer, updateHarmonyLayer } from './harmony-layer.js';
import { initRhythmLayer, updateRhythmLayer } from './rhythm-layer.js';
```

<!-- Riga 145-149: boot V3_MODE block -->
```javascript
  if (CFG.V3_MODE) {
    initMacroComposer();
    initHarmonyLayer();
    initRhythmLayer();
```

<!-- Riga 247-252: midiWorker.onmessage V3_MODE block -->
```javascript
    if (CFG.V3_MODE) {
      // ── v3 layer system: MacroComposer + HarmonyLayer + RhythmLayer ──
      // I composer v2 non vengono chiamati (D-08: rimangono intatti come reference/fallback)
      updateMacroComposer(dt);
      updateHarmonyLayer(dt);
      updateRhythmLayer(dt);
```

<!-- melody-texture-layer.js — exports da collegare -->
```javascript
export function initMelodyTextureLayer() { /* ... */ }
export function updateMelodyTextureLayer(dt) { /* ... */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V3_MODE routing per MelodyTextureLayer in main.js</name>
  <files>MACH:INE II/src/main.js</files>
  <read_first>MACH:INE II/src/main.js</read_first>
  <action>
Tre modifiche chirurgiche a main.js, identiche al pattern usato per RhythmLayer (Phase 2 Plan 03):

**Modifica 1 — Import (dopo riga 29, dopo l'import di rhythm-layer.js):**
Aggiungere:
```javascript
import { initMelodyTextureLayer, updateMelodyTextureLayer } from './melody-texture-layer.js';
```

**Modifica 2 — Init boot (dopo `initRhythmLayer()` nel blocco `if (CFG.V3_MODE)`):**
Aggiungere:
```javascript
    initMelodyTextureLayer();
```
Subito dopo `initRhythmLayer();` e prima di qualsiasi `sendMIDIStart()` o log.

Aggiornare il log di boot da:
```javascript
console.log('[V3] MacroComposer + HarmonyLayer + RhythmLayer initialized');
```
a:
```javascript
console.log('[V3] MacroComposer + HarmonyLayer + RhythmLayer + MelodyTextureLayer initialized');
```

**Modifica 3 — Update nel midiWorker.onmessage (dopo `updateRhythmLayer(dt)`):**
Aggiungere:
```javascript
      updateMelodyTextureLayer(dt);
```
Subito dopo `updateRhythmLayer(dt);` — ordine garantito: MacroComposer -> HarmonyLayer -> RhythmLayer -> MelodyTextureLayer. Il MacroComposer aggiorna `melodicActivity` PRIMA che MelodyTextureLayer lo legga.

**NON modificare** il branch v2 legacy (else block), nessun altro import, nessun'altra logica.

Diff totale atteso: 3 righe aggiunte, 1 riga modificata (log), 0 righe rimosse. File da 312 a ~316 righe.
  </action>
  <verify>
    <automated>
grep -q "updateMelodyTextureLayer" "MACH:INE II/src/main.js" \
  && grep -q "initMelodyTextureLayer" "MACH:INE II/src/main.js" \
  && grep -q "melody-texture-layer" "MACH:INE II/src/main.js"
    </automated>
  </verify>
  <acceptance_criteria>
    - main.js contiene `import { initMelodyTextureLayer, updateMelodyTextureLayer } from './melody-texture-layer.js'`
    - main.js contiene `initMelodyTextureLayer()` dentro il blocco `if (CFG.V3_MODE)` di init
    - main.js contiene `updateMelodyTextureLayer(dt)` dentro il blocco `if (CFG.V3_MODE)` del midiWorker.onmessage
    - `updateMelodyTextureLayer(dt)` appare DOPO `updateRhythmLayer(dt)` (ordine esecuzione)
    - `initMelodyTextureLayer()` appare DOPO `initRhythmLayer()` (ordine init)
    - Il log di boot contiene `MelodyTextureLayer`
    - Il branch v2 legacy (else block con 7 composer engines) e invariato
    - Il file main.js ha meno di 320 righe totali
    - `grep -c "updateComposer(" "MACH:INE II/src/main.js"` rimane invariato (v2 intatto)
  </acceptance_criteria>
  <done>main.js collegato a melody-texture-layer.js: import, init e update nel loop V3_MODE. Ordine esecuzione Worker: MacroComposer -> HarmonyLayer -> RhythmLayer -> MelodyTextureLayer</done>
</task>

</tasks>

<verification>
- `grep "melody-texture-layer" "MACH:INE II/src/main.js"` mostra import e due chiamate
- Ordine nel Worker: updateMacroComposer -> updateHarmonyLayer -> updateRhythmLayer -> updateMelodyTextureLayer (verificabile con grep -n)
- Il branch v2 e intatto: `grep "updateComposer(" "MACH:INE II/src/main.js"` conta lo stesso numero di prima
</verification>

<success_criteria>
MelodyTextureLayer e attivo nel sistema V3_MODE. Il loop di esecuzione Worker esegue tutti e 4 i layer v3 in ordine corretto. Il concerto v3 ora ha MacroComposer + armonia + ritmo + melodia/texture. Pronto per Phase 4 (integrazione e visual system).
</success_criteria>

<output>
After completion, create `.planning/phases/03-melodylayer-e-texturelayer/03-03-SUMMARY.md`
</output>
