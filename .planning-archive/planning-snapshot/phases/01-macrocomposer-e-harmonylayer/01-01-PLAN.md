---
phase: 01-macrocomposer-e-harmonylayer
plan: "01-01"
title: "CFG.MACRO config e MacroComposer state machine"
type: execute
wave: 1
depends_on: []
files_modified:
  - "MACH:INE II/src/config.js"
  - "MACH:INE II/src/macro-composer.js"
autonomous: true
requirements:
  - "MARC-01"
  - "MARC-02"
  - "MARC-03"
must_haves:
  truths:
    - "macroState.arcPercent avanza da 0.0 a 1.0 durante il concerto"
    - "Le 4 dimensioni (rhythmicDensity, harmonicColor, melodicActivity, textureDepth) interpolano tra checkpoint con smooth-step"
    - "I picchi delle 4 dimensioni non coincidono mai: harmonicColor picco a pct ~0.62, rhythmicDensity picco a pct ~0.84, melodicActivity picco a pct ~0.75"
    - "rhythmicDensity scende a 0 per 8 bar alla false-resolution (~min35) e poi risale a valore superiore"
    - "currentMode transita nella sequenza A_lydian -> Bb_phrygian -> D_dorian -> C#_dorian -> E_phrygian -> A_lydian"
    - "Micro-drift EMA oscilla +-7% attorno ai target senza mai creare plateau"
  artifacts:
    - path: "MACH:INE II/src/config.js"
      provides: "CFG.MACRO block con checkpoints, modalSequence, anchor voicings, bpmReference"
      contains: "MACRO:"
    - path: "MACH:INE II/src/macro-composer.js"
      provides: "State machine 4D pura — nessun output MIDI"
      exports: ["macroState", "initMacroComposer", "updateMacroComposer", "getMacroState"]
      min_lines: 80
  key_links:
    - from: "MACH:INE II/src/macro-composer.js"
      to: "MACH:INE II/src/config.js"
      via: "import { CFG } from './config.js'"
      pattern: "CFG\\.MACRO"
    - from: "MACH:INE II/src/macro-composer.js"
      to: "MACH:INE II/src/sequencer.js"
      via: "import { getSequencerStatus } from './sequencer.js'"
      pattern: "getSequencerStatus"
---

# Plan 01-01: CFG.MACRO config e MacroComposer state machine

## Goal

Creare il cuore narrativo del sistema v3: un MacroComposer che governa un arco 4D precomposto (rhythmicDensity, harmonicColor, melodicActivity, textureDepth) su 45 minuti, con picchi staggered (MARC-02) e false-resolution (MARC-03). Il MacroComposer non emette MIDI — esporta solo stato mutable che i layer successivi consumano.

<objective>
Creare CFG.MACRO in config.js con checkpoint array 4D, sequenza modale, e parametri di drift/EMA. Creare macro-composer.js come state machine pura che interpola tra checkpoint usando smooth-step + micro-drift EMA.

Purpose: Fondazione dell'arco narrativo v3 — il "punteggio" precomposto che tutti i layer leggono.
Output: CFG.MACRO block in config.js, src/macro-composer.js con exports {macroState, initMacroComposer, updateMacroComposer, getMacroState}
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-macrocomposer-e-harmonylayer/01-CONTEXT.md
@.planning/phases/01-macrocomposer-e-harmonylayer/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From MACH:INE II/src/sequencer.js (line 151):
```javascript
export function getSequencerStatus() {
  if (!active) return { active: false };
  // ...
  return {
    active: true,
    engine: label,        // string: "TERRENO +1" etc.
    act,                  // string: act name
    step: cueIndex,       // number
    total: CUES.length,   // number
    elapsed: Math.floor(globalTime),  // seconds
    duration: CUES[CUES.length - 1].t, // 3000 (seconds)
    transitioning: transitions.length > 0,
    progress: globalTime / CUES[CUES.length - 1].t, // 0.0-1.0
    paused,
    looping,
  };
}
```

From MACH:INE II/src/config.js (line 6-8):
```javascript
export const CFG = {
  V3_MODE: false,       // false = v2 behavior (7 motori); true = v3 layer system (Phase 1+)
  bpmLerpBeats: 2,      // BPM transition lerp duration in beats
  // ... rest of config, ends at line 403 with closing `};`
};
```

From MACH:INE II/src/composer.js (pattern di riferimento, lines 16-21):
```javascript
function sendMIDINote(ch, note, vel, dur) {
  const pm = getPresenceMultiplier('terreno');
  if (pm < 0.05) return;
  if (!isChannelAllowed('terreno', ch)) return;
  _rawSend(ch, note, Math.max(1, Math.round(vel * pm)), dur);
}
```

EMA pattern (from state.js / audio.js):
```javascript
val += (target - val) * Math.min(1, dt / tau);
```

Smooth-step pattern (from sequencer.js):
```javascript
const ease = progress * progress * (3 - 2 * progress);
const value = from + (to - from) * ease;
```
</interfaces>
</context>

<tasks>

<task id="01-01-01" type="auto">
  <name>Task 1: CFG.MACRO configuration block in config.js</name>
  <files>MACH:INE II/src/config.js</files>
  <read_first>
    - MACH:INE II/src/config.js — stato corrente, struttura CFG, dove aggiungere MACRO (prima della chiusura `};` a riga ~403)
    - .planning/phases/01-macrocomposer-e-harmonylayer/01-CONTEXT.md — decisioni D-01 through D-16
    - .planning/phases/01-macrocomposer-e-harmonylayer/01-RESEARCH.md — checkpoint array di riferimento, anti-pattern, MODE_DRONE mapping
  </read_first>
  <action>
Aggiungere il blocco `MACRO` all'oggetto `CFG` in config.js, PRIMA della chiusura `};` finale. Il blocco contiene:

1. **Parametri base:**
   - `concertDurationSec: 2700` — 45 minuti (durata target v3, per D-01)
   - `bpmReference: 88` — BPM di riferimento per calcolo bar duration in HarmonyLayer
   - `microDriftAmp: 0.07` — ampiezza oscillazione ±7% attorno al target (per D-02)
   - `microDriftFreqSec: 23` — periodo dell'oscillazione in secondi (numero primo per asimmetria)
   - `emaTau: 4.0` — time constant EMA per smoothing valori 4D in secondi

2. **Checkpoint array** (per D-01, D-03, D-04). Ogni checkpoint ha: `pct` (0.0-1.0 del concerto), le 4 dimensioni (0.0-1.0), e `mode` (stringa). I valori pct sono calcolati su 45 min = 2700s:
   ```javascript
   checkpoints: [
     { pct: 0.00, rD: 0.0, hC: 0.1, mA: 0.0, tD: 0.1, mode: 'A_lydian' },
     { pct: 0.22, rD: 0.1, hC: 0.3, mA: 0.1, tD: 0.2, mode: 'A_lydian' },
     { pct: 0.44, rD: 0.3, hC: 0.7, mA: 0.4, tD: 0.4, mode: 'Bb_phrygian' },
     { pct: 0.62, rD: 0.5, hC: 1.0, mA: 0.5, tD: 0.5, mode: 'Bb_phrygian' },    // harmonicColor PEAK ~min28
     { pct: 0.73, rD: 0.7, hC: 0.7, mA: 0.6, tD: 0.6, mode: 'D_dorian' },        // density building
     { pct: 0.75, rD: 0.0, hC: 0.5, mA: 0.3, tD: 0.4, mode: 'D_dorian', instant: true },  // FALSE RESOLUTION start (rD drops to 0)
     { pct: 0.78, rD: 0.0, hC: 0.5, mA: 0.3, tD: 0.4, mode: 'D_dorian' },        // FALSE RESOLUTION hold (~8 bar at 88bpm ≈ 22s ≈ 0.8% of 2700s)
     { pct: 0.80, rD: 0.9, hC: 0.6, mA: 0.7, tD: 0.7, mode: 'C#_dorian' },       // rebound above previous
     { pct: 0.84, rD: 1.0, hC: 0.6, mA: 0.8, tD: 0.7, mode: 'C#_dorian' },       // rhythmicDensity PEAK ~min38
     { pct: 0.89, rD: 0.5, hC: 0.4, mA: 0.5, tD: 0.5, mode: 'E_phrygian' },
     { pct: 0.95, rD: 0.2, hC: 0.2, mA: 0.1, tD: 0.2, mode: 'A_lydian' },        // dissoluzione
     { pct: 1.00, rD: 0.0, hC: 0.0, mA: 0.0, tD: 0.0, mode: 'A_lydian' },        // fine
   ],
   ```
   **NOTA CRITICA sulle percentuali:** ROADMAP dice ~min28 per harmonicColor peak. A 45min concerto: min28/45 = 0.622 → `pct: 0.62`. Densita ~min33 = 33/45 = 0.733 → `pct: 0.73`. False-resolution ~min35 = 35/45 = 0.778 → `pct: 0.75-0.78`. RhythmicDensity peak ~min38 = 38/45 = 0.844 → `pct: 0.84`. (per D-03)

   **NOTA `instant: true`** sul checkpoint false-resolution: il MacroComposer deve bypassare l'EMA e impostare direttamente `rhythmicDensity = 0` su questo checkpoint. Questo evita il Pitfall 5 della RESEARCH (EMA tau=4s troppo lento per la discesa a zero).

3. **Sequenza modale** (per D-10):
   ```javascript
   modalSequence: ['A_lydian', 'Bb_phrygian', 'D_dorian', 'C#_dorian', 'E_phrygian', 'A_lydian'],
   ```

4. **Scale note sets** per ogni modo (come MODES in composer.js). Note MIDI diatoniche estese (2 ottave minimo):
   ```javascript
   modes: {
     A_lydian:     [45, 57, 59, 61, 63, 64, 66, 68, 69, 71, 73, 75, 76, 78, 80, 81],  // A3=57, #4=D#
     Bb_phrygian:  [46, 58, 59, 61, 63, 65, 66, 68, 70, 71, 73, 75, 77, 78, 80, 82],  // Bb3=58, b2=Cb=B
     D_dorian:     [38, 50, 52, 53, 55, 57, 59, 60, 62, 64, 65, 67, 69, 71, 72, 74],  // D3=50, natural B
     C#_dorian:    [37, 49, 51, 52, 54, 56, 58, 59, 61, 63, 64, 66, 68, 70, 71, 73],  // C#3=49, natural A#
     E_phrygian:   [40, 52, 53, 55, 57, 59, 60, 62, 64, 65, 67, 69, 71, 72, 74, 76],  // E3=52, b2=F
   },
   ```

5. **Pentatonic subsets** per ogni modo (per HARM-05 — usati da HarmonyLayer in Plan 02):
   ```javascript
   pentatonic: {
     A_lydian:    [57, 59, 61, 64, 66, 69, 71, 73, 76, 78],  // A B C# E F# (no #4, no 7)
     Bb_phrygian: [58, 61, 63, 65, 68, 70, 73, 75, 77, 80],  // Bb Db Eb F Ab
     D_dorian:    [50, 52, 55, 57, 59, 62, 64, 67, 69, 71],  // D E G A B
     C#_dorian:   [49, 51, 54, 56, 58, 61, 63, 66, 68, 70],  // C# D# F# G# A#
     E_phrygian:  [52, 55, 57, 59, 62, 64, 67, 69, 71, 74],  // E G A B D
   },
   ```

6. **Drone root mapping** (per HARM-01, D-15):
   ```javascript
   droneRoot: {
     A_lydian:    57,   // A3 — root
     Bb_phrygian: 57,   // A3 — anchor condiviso (modal interchange sopra A)
     D_dorian:    50,   // D3 — root
     C#_dorian:   57,   // A3 — continuita' armonica tra D e E
     E_phrygian:  57,   // A3 — E Phrygian su radice A con colore frigio
   },
   ```

7. **Pivot notes** per transizioni modali (per D-11):
   ```javascript
   pivotNotes: {
     'A_lydian->Bb_phrygian':   57,  // A3
     'Bb_phrygian->D_dorian':   62,  // D4
     'D_dorian->C#_dorian':     57,  // A3 (nota comune)
     'C#_dorian->E_phrygian':   64,  // E4
     'E_phrygian->A_lydian':    57,  // A3
   },
   ```

8. **Anchor voicings** per modo (per D-13). 3 voicings per modo — apertura, pivot, picco. Formato: array di oggetti `{ ch2: [note, note], ch4: [note, note, note], bass: note }`:
   ```javascript
   anchors: {
     A_lydian: [
       { bass: 45, ch2: [57, 69], ch4: [64, 66, 69] },       // apertura: A2 bass, A3+A4 drone, E4+F#4+A4 triade
       { bass: 45, ch2: [57, 68], ch4: [61, 64, 68] },       // pivot: A2, A3+Ab4, C#4+E4+Ab4 (tensione lydian)
       { bass: 45, ch2: [57, 69], ch4: [66, 69, 73] },       // picco: A2, A3+A4, F#4+A4+C#5
     ],
     Bb_phrygian: [
       { bass: 46, ch2: [57, 70], ch4: [63, 65, 70] },       // apertura: Bb2, A3+Bb4, Eb4+F4+Bb4
       { bass: 46, ch2: [57, 70], ch4: [65, 68, 70] },       // pivot: Bb2, A3+Bb4, F4+Ab4+Bb4
       { bass: 46, ch2: [57, 70], ch4: [63, 66, 70] },       // picco: Bb2, A3+Bb4, Eb4+F#4+Bb4
     ],
     D_dorian: [
       { bass: 38, ch2: [50, 62], ch4: [57, 60, 62] },       // apertura: D2, D3+D4, A3+C4+D4
       { bass: 38, ch2: [50, 62], ch4: [59, 62, 65] },       // pivot: D2, D3+D4, B3+D4+F4
       { bass: 38, ch2: [50, 62], ch4: [57, 60, 64] },       // picco: D2, D3+D4, A3+C4+E4 (color dorico: B naturale)
     ],
     C#_dorian: [
       { bass: 37, ch2: [57, 61], ch4: [56, 58, 61] },       // apertura: C#2, A3+C#4, G#3+A#3+C#4
       { bass: 37, ch2: [57, 61], ch4: [58, 61, 64] },       // pivot: C#2, A3+C#4, A#3+C#4+E4
       { bass: 37, ch2: [57, 61], ch4: [56, 59, 63] },       // picco: C#2, A3+C#4, G#3+B3+Eb4
     ],
     E_phrygian: [
       { bass: 40, ch2: [57, 64], ch4: [59, 62, 64] },       // apertura: E2, A3+E4, B3+D4+E4
       { bass: 40, ch2: [57, 64], ch4: [60, 64, 67] },       // pivot: E2, A3+E4, C4+E4+G4
       { bass: 40, ch2: [57, 64], ch4: [59, 62, 65] },       // picco: E2, A3+E4, B3+D4+F4
     ],
   },
   ```
   **NOTA:** Questi sono valori di partenza plausibili — calibrare via ascolto su setup MIDI hardware (Claude's Discretion da D-13). CH2 notes sono sempre >=9 semitoni sopra bass per HARM-03. CH4 notes rispettano voice leading <=3 semitoni tra anchors consecutivi per HARM-04.

Dopo la modifica, config.js deve restare un file valido ES module con `export const CFG = { ... };` — nessun import aggiuntivo necessario.
  </action>
  <verify>
    <automated>cd "MACH:INE II" && node -e "import('./src/config.js').then(m => { const c = m.CFG.MACRO; console.log('checkpoints:', c.checkpoints.length); console.log('modes:', Object.keys(c.modes).length); console.log('concertDurationSec:', c.concertDurationSec); console.log('bpmReference:', c.bpmReference); console.log('droneRoot keys:', Object.keys(c.droneRoot).join(',')); console.log('OK'); }).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] config.js contains `MACRO:` as a property of CFG
    - [ ] config.js contains `concertDurationSec: 2700`
    - [ ] config.js contains `bpmReference: 88`
    - [ ] config.js contains `checkpoints:` with at least 10 entries
    - [ ] config.js contains `instant: true` on the false-resolution checkpoint
    - [ ] config.js contains `modalSequence:` array with 6 entries
    - [ ] config.js contains `modes:` with keys A_lydian, Bb_phrygian, D_dorian, C#_dorian, E_phrygian
    - [ ] config.js contains `pentatonic:` with same 5 mode keys
    - [ ] config.js contains `droneRoot:` with same 5 mode keys
    - [ ] config.js contains `pivotNotes:` with 5 transition keys
    - [ ] config.js contains `anchors:` with same 5 mode keys, each having 3 entries
    - [ ] config.js contains `microDriftAmp: 0.07`
    - [ ] config.js contains `emaTau: 4.0`
    - [ ] The checkpoint at pct ~0.62 has hC: 1.0 (harmonicColor peak, per MARC-02)
    - [ ] The checkpoint at pct ~0.75 has rD: 0.0 and instant: true (false-resolution, per MARC-03)
    - [ ] The checkpoint at pct ~0.84 has rD: 1.0 (rhythmicDensity peak, per MARC-02)
    - [ ] `node -e "import('./src/config.js')"` succeeds (valid ES module syntax)
  </acceptance_criteria>
  <done>CFG.MACRO block presente in config.js con tutti i dati necessari per l'arco narrativo 4D, le scale modali, i drone roots, e gli anchor voicings. Il file e' un ES module valido.</done>
</task>

<task id="01-01-02" type="auto">
  <name>Task 2: macro-composer.js — state machine 4D pura</name>
  <files>MACH:INE II/src/macro-composer.js</files>
  <read_first>
    - MACH:INE II/src/config.js — CFG.MACRO appena creato (task 1), struttura checkpoint
    - MACH:INE II/src/sequencer.js — getSequencerStatus() API (lines 151-171): ritorna { active, elapsed, duration, progress }
    - MACH:INE II/src/composer.js — pattern architetturale da seguire: init/update/toggle/status exports (lines 1-25)
    - .planning/phases/01-macrocomposer-e-harmonylayer/01-RESEARCH.md — Pattern 1 (state machine), Pattern 2 (checkpoint interpolation), Pitfall 1 (sequencer inactive), Pitfall 5 (EMA troppo lento per false-resolution)
  </read_first>
  <action>
Creare `MACH:INE II/src/macro-composer.js` come ES module puro. Il modulo e' una state machine che:
- NON emette MIDI (nessun import di midi.js)
- Esporta `macroState` (oggetto mutable), `initMacroComposer()`, `updateMacroComposer(dt)`, `getMacroState()`

**Struttura del file:**

```
// Box comment header (stile MACH:INE II)
// ═══════════════════════════════════════════════════════════
//  MACH:INE II — MacroComposer v3
//  Arco narrativo 4D precomposto — state machine pura, nessun MIDI
// ═══════════════════════════════════════════════════════════

import { CFG } from './config.js';
import { getSequencerStatus } from './sequencer.js';
```

**1. Stato esportato (mutable):**
```javascript
export const macroState = {
  rhythmicDensity: 0,    // 0.0-1.0
  harmonicColor:   0,    // 0.0-1.0
  melodicActivity: 0,    // 0.0-1.0
  textureDepth:    0,    // 0.0-1.0
  currentMode:     'A_lydian',
  prevMode:        'A_lydian',
  modeProgress:    0,    // 0.0-1.0 within current modal segment
  pivotActive:     false,
  pivotNote:       57,   // MIDI note della pivot condivisa
  arcPercent:      0,    // 0.0-1.0 del concerto
  barClock:        0,    // bar count accumulato (per HarmonyLayer)
};
```

**2. Variabili interne (non esportate):**
- `_internalClock = 0` — accumulatore dt in secondi (fallback se sequencer inattivo, Pitfall 1)
- `_driftPhase = 0` — fase dell'oscillazione micro-drift
- `_prevCheckpointIdx = 0` — cache indice ultimo checkpoint trovato

**3. `initMacroComposer()`:**
- Reset macroState a valori iniziali (tutti a 0, mode A_lydian)
- Reset _internalClock = 0, _driftPhase = 0, _prevCheckpointIdx = 0
- `console.log('[MACRO] init — concertDuration:', CFG.MACRO.concertDurationSec, 's')`

**4. `updateMacroComposer(dt)` — chiamato ogni ~2ms dal Worker:**

Step A — Calcolo arcPercent:
```javascript
const status = getSequencerStatus();
if (status.active) {
  macroState.arcPercent = status.progress;
} else {
  _internalClock += dt / 1000;
  macroState.arcPercent = Math.min(1, _internalClock / CFG.MACRO.concertDurationSec);
}
const pct = macroState.arcPercent;
```

Step B — Trova i 2 checkpoint adiacenti:
```javascript
const cps = CFG.MACRO.checkpoints;
let lo = 0, hi = 1;
for (let i = 1; i < cps.length; i++) {
  if (cps[i].pct >= pct) { hi = i; lo = i - 1; break; }
}
if (pct >= cps[cps.length - 1].pct) { lo = cps.length - 2; hi = cps.length - 1; }
```

Step C — Interpolazione smooth-step tra checkpoint:
```javascript
const cpLo = cps[lo], cpHi = cps[hi];
const segLen = cpHi.pct - cpLo.pct;
const segProgress = segLen > 0 ? (pct - cpLo.pct) / segLen : 1;
// Smooth-step (cubic hermite) per D-01 "easing curvo"
const ease = segProgress * segProgress * (3 - 2 * segProgress);

const targetRD = cpLo.rD + (cpHi.rD - cpLo.rD) * ease;
const targetHC = cpLo.hC + (cpHi.hC - cpLo.hC) * ease;
const targetMA = cpLo.mA + (cpHi.mA - cpLo.mA) * ease;
const targetTD = cpLo.tD + (cpHi.tD - cpLo.tD) * ease;
```

Step D — Micro-drift (per D-02):
```javascript
_driftPhase += (dt / 1000) / CFG.MACRO.microDriftFreqSec;
const drift = Math.sin(_driftPhase * Math.PI * 2) * CFG.MACRO.microDriftAmp;
```

Step E — EMA smoothing con bypass per `instant` (per D-02, Pitfall 5):
```javascript
const dtSec = dt / 1000;
const tau = CFG.MACRO.emaTau;
const alpha = Math.min(1, dtSec / tau);

if (cpLo.instant || cpHi.instant) {
  // Bypass EMA per false-resolution (Pitfall 5 — EMA troppo lenta per discesa a zero)
  macroState.rhythmicDensity = targetRD;
} else {
  macroState.rhythmicDensity += ((targetRD + drift) - macroState.rhythmicDensity) * alpha;
}
macroState.harmonicColor   += ((targetHC + drift * 0.7) - macroState.harmonicColor)   * alpha;
macroState.melodicActivity += ((targetMA + drift * 0.5) - macroState.melodicActivity) * alpha;
macroState.textureDepth    += ((targetTD + drift * 0.8) - macroState.textureDepth)    * alpha;
```
Nota: il drift ha fattori diversi per ogni dimensione (0.7, 0.5, 0.8, 1.0) per evitare che le 4 curve oscillino in sincrono — aggiunge stagger microgestuale.

Step F — Clamp valori a [0.0, 1.0]:
```javascript
macroState.rhythmicDensity = Math.max(0, Math.min(1, macroState.rhythmicDensity));
macroState.harmonicColor   = Math.max(0, Math.min(1, macroState.harmonicColor));
macroState.melodicActivity = Math.max(0, Math.min(1, macroState.melodicActivity));
macroState.textureDepth    = Math.max(0, Math.min(1, macroState.textureDepth));
```

Step G — Aggiornamento modo e pivot:
```javascript
const targetMode = cpHi.mode;
if (targetMode !== macroState.currentMode) {
  macroState.prevMode = macroState.currentMode;
  macroState.currentMode = targetMode;
  // Attiva finestra pivot di 1 bar (D-11)
  macroState.pivotActive = true;
  const transitionKey = macroState.prevMode + '->' + targetMode;
  macroState.pivotNote = CFG.MACRO.pivotNotes[transitionKey] || 57;
  console.log('[MACRO] mode transition:', macroState.prevMode, '->', targetMode, 'pivot:', macroState.pivotNote);
}
```

Step H — Contatore bar (per HarmonyLayer):
```javascript
const barsPerSec = CFG.MACRO.bpmReference / 60 / 4;
macroState.barClock += (dt / 1000) * barsPerSec;
```

Step I — Disattiva pivot dopo 1 bar:
La logica pivot dura 1 bar. Usare un contatore interno `_pivotBarStart`:
```javascript
if (macroState.pivotActive && (macroState.barClock - _pivotBarStart) >= 1) {
  macroState.pivotActive = false;
}
```
Quando si attiva il pivot (Step G), salvare `_pivotBarStart = macroState.barClock`.

Step J — Debug log (ogni ~60 secondi, solo se CFG.debug === true):
```javascript
if (CFG.debug && Math.floor(pct * 100) % 2 === 0 && Math.floor(pct * 100) !== _lastLogPct) {
  _lastLogPct = Math.floor(pct * 100);
  console.log('[MACRO]', JSON.stringify({
    pct: pct.toFixed(3),
    rD: macroState.rhythmicDensity.toFixed(2),
    hC: macroState.harmonicColor.toFixed(2),
    mA: macroState.melodicActivity.toFixed(2),
    tD: macroState.textureDepth.toFixed(2),
    mode: macroState.currentMode,
    pivot: macroState.pivotActive,
  }));
}
```

**5. `getMacroState()` — convenience getter:**
```javascript
export function getMacroState() { return macroState; }
```

**Regole di stile:**
- Box comment header identico allo stile MACH:INE II (vedi composer.js)
- Commenti tecnici in inglese, commenti musicali in italiano (per CLAUDE.md)
- Nessun magic number — tutti i valori da CFG.MACRO
- File sotto 200 righe (ben sotto il limite di 500)
  </action>
  <verify>
    <automated>cd "MACH:INE II" && node -e "import('./src/macro-composer.js').then(m => { console.log('macroState:', typeof m.macroState); console.log('init:', typeof m.initMacroComposer); console.log('update:', typeof m.updateMacroComposer); console.log('get:', typeof m.getMacroState); m.initMacroComposer(); console.log('post-init mode:', m.macroState.currentMode); console.log('OK'); }).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] File MACH:INE II/src/macro-composer.js exists
    - [ ] macro-composer.js contains `export const macroState =`
    - [ ] macro-composer.js contains `export function initMacroComposer(`
    - [ ] macro-composer.js contains `export function updateMacroComposer(dt)`
    - [ ] macro-composer.js contains `export function getMacroState(`
    - [ ] macro-composer.js contains `import { CFG } from './config.js'`
    - [ ] macro-composer.js contains `import { getSequencerStatus } from './sequencer.js'`
    - [ ] macro-composer.js contains `CFG.MACRO.checkpoints` (reads checkpoint array)
    - [ ] macro-composer.js contains `segProgress * segProgress * (3 - 2 * segProgress)` (smooth-step)
    - [ ] macro-composer.js contains `Math.sin` (micro-drift oscillation)
    - [ ] macro-composer.js contains `instant` (instant bypass for false-resolution)
    - [ ] macro-composer.js contains `pivotActive` (pivot window management)
    - [ ] macro-composer.js contains `_internalClock` (fallback clock when sequencer inactive)
    - [ ] macro-composer.js does NOT contain `sendMIDINote` (state machine pura, nessun MIDI output)
    - [ ] macro-composer.js does NOT contain `import.*midi` (no midi.js dependency)
    - [ ] `node -e "import('./src/macro-composer.js')"` succeeds (valid ES module)
    - [ ] File is under 250 lines (wc -l)
  </acceptance_criteria>
  <done>macro-composer.js esiste come state machine 4D pura: interpola tra checkpoint con smooth-step, applica micro-drift EMA, gestisce sequenza modale con finestra pivot, espone macroState per i layer downstream. Nessun output MIDI. initMacroComposer() e updateMacroComposer(dt) funzionano senza errori.</done>
</task>

</tasks>

<verification>
1. `cd "MACH:INE II" && node -e "import('./src/config.js').then(m => console.log('MACRO keys:', Object.keys(m.CFG.MACRO).join(',')))"` — verifica che CFG.MACRO contenga tutte le chiavi attese
2. `cd "MACH:INE II" && node -e "import('./src/macro-composer.js').then(m => { m.initMacroComposer(); m.updateMacroComposer(16); console.log('arcPct:', m.macroState.arcPercent, 'mode:', m.macroState.currentMode); })"` — verifica che l'update funzioni (arcPercent avanza, mode e' A_lydian all'inizio)
3. Verifica manuale: aprire browser, attivare sequencer con `0`, osservare console per i log `[MACRO]` che mostrano l'evoluzione 4D
</verification>

<success_criteria>
- CFG.MACRO block in config.js con checkpoint array completo, scale modali, drone roots, pivot notes, anchor voicings
- macro-composer.js esporta macroState con le 4 dimensioni + currentMode + pivotActive + barClock
- L'interpolazione smooth-step produce valori 4D che evolvono continuamente (no plateau grazie a micro-drift)
- I picchi harmonicColor (~pct 0.62), density (~pct 0.73), rhythmicDensity (~pct 0.84) non coincidono (MARC-02)
- La false-resolution a ~pct 0.75 porta rhythmicDensity a 0 istantaneamente (MARC-03)
- La sequenza modale segue A_lydian -> Bb_phrygian -> D_dorian -> C#_dorian -> E_phrygian -> A_lydian (D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/01-macrocomposer-e-harmonylayer/01-01-SUMMARY.md`
</output>
