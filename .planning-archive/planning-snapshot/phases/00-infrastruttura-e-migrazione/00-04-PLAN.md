---
phase: 00-infrastruttura-e-migrazione
plan: 04
type: execute
wave: 2
depends_on:
  - "00-01"
files_modified:
  - "MACH:INE II/src/sequencer.js"
autonomous: true
requirements:
  - INFR-03
must_haves:
  truths:
    - "Al cue 'silence', tutti i canali MIDI 0-7 ricevono CC 123 prima che gli engine vengano disattivati"
    - "Al cue 'end', stessa protezione — nessun burst di note alla fine del concerto"
    - "sendMIDIAllNotesOff è importata da midi.js in sequencer.js"
  artifacts:
    - path: "MACH:INE II/src/sequencer.js"
      provides: "flush MIDI prima di deactivateAll"
      contains: "sendMIDIAllNotesOff"
  key_links:
    - from: "MACH:INE II/src/sequencer.js processCue('silence')"
      to: "sendMIDIAllNotesOff()"
      via: "prima istruzione del case 'silence'"
      pattern: "sendMIDIAllNotesOff\\(\\)"
    - from: "MACH:INE II/src/sequencer.js processCue('end')"
      to: "sendMIDIAllNotesOff()"
      via: "prima istruzione del case 'end'"
      pattern: "sendMIDIAllNotesOff\\(\\)"
---

<objective>
Aggiunge `sendMIDIAllNotesOff()` come prima istruzione nei case 'silence' e 'end' di `processCue()` in sequencer.js.

Purpose: Le transizioni di sezione chiamano `_deactivateAll()` che stoppa la generazione futura di note, ma non flasha le note già schedulate nel WebMIDI driver. Senza CC 123, note lunghe o in attesa restano attive sul synth producendo burst udibili. La funzione `sendMIDIAllNotesOff()` già esiste in midi.js (riga 151) — basta importarla e chiamarla. L'ordine è critico: flush prima di deactivate. Soddisfa INFR-03.
Output: Una riga import aggiunta in sequencer.js, due chiamate `sendMIDIAllNotesOff()` aggiunte in processCue.
</objective>

<execution_context>
@/Users/Edo_1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Edo_1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Edo_1/MACH-INE II/.planning/ROADMAP.md
@/Users/Edo_1/MACH-INE II/.planning/phases/00-infrastruttura-e-migrazione/00-01-SUMMARY.md

<interfaces>
<!-- Da MACH:INE II/src/sequencer.js — import block attuale (righe 1-9) -->
```javascript
import { executeMutation, requestFraming, requestCameraShake } from './director.js';
import { startInvertDissolve, setConcertTime } from './colors.js';
import { setPresenceMultiplier, getPresenceMultiplier, resetAllMultipliers } from './presence-multiplier.js';
```

<!-- Da MACH:INE II/src/midi.js — funzione da importare (riga 151 circa) -->
```javascript
export function sendMIDIAllNotesOff() {
  // sends CC 123 (All Notes Off) on channels 0-7
}
```

<!-- Da MACH:INE II/src/sequencer.js — processCue() attuale (righe 346-398) -->
```javascript
function processCue(cue) {
  switch (cue.action) {
    case 'silence':
      if (_deactivateAll) _deactivateAll();   // ← note burst: no flush prima
      resetAllMultipliers();
      transitions = [];
      break;

    // ...

    case 'end':
      if (_deactivateAll) _deactivateAll();   // ← stessa vulnerabilità
      resetAllMultipliers();
      stopSequencer();
      console.log('[SEQUENCER] PERFORMANCE COMPLETE');
      break;
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Importa sendMIDIAllNotesOff e aggiungi flush nei cue 'silence' e 'end' (INFR-03)</name>
  <read_first>
    - Leggi `/Users/Edo_1/MACH-INE II/MACH:INE II/src/sequencer.js` righe 1-15 per vedere il blocco import attuale
    - Leggi `/Users/Edo_1/MACH-INE II/MACH:INE II/src/sequencer.js` righe 344-400 per vedere processCue completo
  </read_first>
  <files>MACH:INE II/src/sequencer.js</files>
  <action>
    Due modifiche chirurgiche in `sequencer.js`:

    **Modifica A — Aggiunta import**

    Trovare la terza riga import (circa riga 9):
    ```javascript
    import { setPresenceMultiplier, getPresenceMultiplier, resetAllMultipliers } from './presence-multiplier.js';
    ```

    Aggiungere UNA RIGA DOPO (non modificare la riga esistente):
    ```javascript
    import { sendMIDIAllNotesOff } from './midi.js';
    ```

    **Modifica B — case 'silence' in processCue()**

    Trovare (circa riga 348-352):
    ```javascript
    case 'silence':
      if (_deactivateAll) _deactivateAll();
      resetAllMultipliers();
      transitions = [];
      break;
    ```

    Sostituire con:
    ```javascript
    case 'silence':
      sendMIDIAllNotesOff(); // flush all scheduled notes before deactivating engines
      if (_deactivateAll) _deactivateAll();
      resetAllMultipliers();
      transitions = [];
      break;
    ```

    **Modifica C — case 'end' in processCue()**

    Trovare (circa riga 392-397):
    ```javascript
    case 'end':
      if (_deactivateAll) _deactivateAll();
      resetAllMultipliers();
      stopSequencer();
      console.log('[SEQUENCER] PERFORMANCE COMPLETE');
      break;
    ```

    Sostituire con:
    ```javascript
    case 'end':
      sendMIDIAllNotesOff(); // flush all scheduled notes before stopping
      if (_deactivateAll) _deactivateAll();
      resetAllMultipliers();
      stopSequencer();
      console.log('[SEQUENCER] PERFORMANCE COMPLETE');
      break;
    ```

    ORDINE OBBLIGATORIO: `sendMIDIAllNotesOff()` DEVE essere la PRIMA istruzione in ogni case. Se chiamata dopo `_deactivateAll()`, note con timestamp futuro già nel buffer WebMIDI vengono suonate ugualmente (il driver le consegna prima che CC 123 arrivi). Flush prima = corretto.

    Nessun'altra modifica al file.
  </action>
  <acceptance_criteria>
    - `grep -n "sendMIDIAllNotesOff" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/sequencer.js"` restituisce esattamente 3 righe (1 import + 2 chiamate)
    - `grep -n "import.*midi" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/sequencer.js"` restituisce una riga contenente `sendMIDIAllNotesOff`
    - La chiamata `sendMIDIAllNotesOff()` nel case 'silence' appare PRIMA della riga `if (_deactivateAll)` — verificare con numeri di riga
    - La chiamata `sendMIDIAllNotesOff()` nel case 'end' appare PRIMA della riga `if (_deactivateAll)` — verificare con numeri di riga
  </acceptance_criteria>
  <verify>
    <automated>grep -n "sendMIDIAllNotesOff\|_deactivateAll" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/sequencer.js"</automated>
  </verify>
  <done>sequencer.js importa `sendMIDIAllNotesOff` da `./midi.js`. Nei case 'silence' e 'end', `sendMIDIAllNotesOff()` è la prima istruzione, prima di `_deactivateAll()`. Nessun'altra riga modificata.</done>
</task>

</tasks>

<verification>
1. `grep -c "sendMIDIAllNotesOff" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/sequencer.js"` — output: `3`
2. Apertura `http://localhost:8282` in Chrome — nessun errore console (import valido)
3. Verifica manuale:
   - Avviare sequencer con un engine attivo (tasti keyboard)
   - Premere Shift+0 (o equivalente sequencer stop) — MIDI monitor deve mostrare silenziamento immediato senza burst di note
   - Il synth deve silenziare immediatamente a tutti i canali
</verification>

<success_criteria>
- 3 occorrenze di `sendMIDIAllNotesOff` in sequencer.js (import + 2 chiamate)
- In entrambi i case, `sendMIDIAllNotesOff()` precede `_deactivateAll()`
- Nessun errore ES module al boot
- Su MIDI monitor: silenzio immediato e pulito alle transizioni di sezione, nessun burst di note
</success_criteria>

<output>
Dopo il completamento, crea `.planning/phases/00-infrastruttura-e-migrazione/00-04-SUMMARY.md`
</output>
