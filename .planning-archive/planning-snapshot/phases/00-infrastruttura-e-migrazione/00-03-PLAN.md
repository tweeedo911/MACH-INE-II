---
phase: 00-infrastruttura-e-migrazione
plan: 03
type: execute
wave: 2
depends_on:
  - "00-01"
files_modified:
  - "MACH:INE II/src/config.js"
  - "MACH:INE II/src/main.js"
autonomous: true
requirements:
  - INFR-02
must_haves:
  truths:
    - "Le transizioni di BPM lerpano gradualmente su 2 battute — nessun snap brusco"
    - "Il lerp non produce NaN quando DERIVA (bpm: null) è l'unico engine attivo"
    - "Il BPM lerp è espresso in battute (CFG.bpmLerpBeats) non in millisecondi fissi"
  artifacts:
    - path: "MACH:INE II/src/config.js"
      provides: "parametro bpmLerpBeats"
      contains: "bpmLerpBeats: 2"
    - path: "MACH:INE II/src/main.js"
      provides: "variabili lerp + logica lerp nel handler"
      contains: "_targetClockBpm"
  key_links:
    - from: "MACH:INE II/src/main.js getActiveBpm()"
      to: "_targetClockBpm"
      via: "assignment invece di lastClockBpm"
      pattern: "_targetClockBpm = e\\.bpm"
    - from: "MACH:INE II/src/main.js midiWorker.onmessage"
      to: "updateMIDIClock(_currentClockBpm)"
      via: "lerp _currentClockBpm prima di chiamare updateMIDIClock"
      pattern: "_currentClockBpm.*_targetClockBpm"
---

<objective>
Aggiunge BPM lerp su 2 battute alle transizioni di engine in main.js, con il parametro `bpmLerpBeats` in config.js.

Purpose: `getActiveBpm()` attualmente snappa il BPM istantaneamente (`lastClockBpm = e.bpm`). Questo rompe il sync Ableton alle transizioni di engine. Il lerp espresso in battute è tempo-relativo: 2 battute a 80 BPM = 1.5s, a 128 BPM = 0.94s — corretto in entrambi i casi. Soddisfa INFR-02.
Output: `CFG.bpmLerpBeats` in config.js, `_targetClockBpm`/`_currentClockBpm` in main.js, logica lerp nel `midiWorker.onmessage` handler.
</objective>

<execution_context>
@/Users/Edo_1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Edo_1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Edo_1/MACH-INE II/.planning/ROADMAP.md
@/Users/Edo_1/MACH-INE II/.planning/phases/00-infrastruttura-e-migrazione/00-01-SUMMARY.md

<interfaces>
<!-- Da MACH:INE II/src/main.js — stato attuale (righe 199-251) -->
```javascript
let lastClockBpm = 120;
let wasAnyComposerActive = false;

function getActiveBpm() {
  const engines = [
    { active: composerActive,  bpm: CFG.COMPOSER.bpm,  pm: getPresenceMultiplier('terreno') },
    { active: composer2Active, bpm: CFG.COMPOSER2.bpm, pm: getPresenceMultiplier('meccanica') },
    { active: composer3Active, bpm: CFG.COMPOSER3.bpm, pm: getPresenceMultiplier('deriva') },   // bpm: null
    { active: composer4Active, bpm: CFG.COMPOSER4.bpm, pm: getPresenceMultiplier('vortice') },
    { active: composer5Active, bpm: CFG.COMPOSER5.bpm, pm: getPresenceMultiplier('cristallo') },
    { active: composer6Active, bpm: CFG.COMPOSER6.bpm, pm: getPresenceMultiplier('abisso') },
    { active: composer7Active, bpm: CFG.COMPOSER7.bpm, pm: getPresenceMultiplier('solco') },
  ];
  let bestPm = 0;
  for (const e of engines) {
    if (e.active && e.bpm && e.pm > bestPm) {  // guard: e.bpm già skippa null
      bestPm = e.pm;
      lastClockBpm = e.bpm;  // PROBLEMA: snap diretto
    }
  }
  return lastClockBpm;
}

midiWorker.onmessage = ({ data: { dt } }) => {
  try {
    if (!running) return;
    // ... (righe 226-244) ...
    if (anyActive) updateMIDIClock(getActiveBpm());  // riga 245
  } catch (e) {
    console.error('[MIDI CLOCK] Handler error (clock kept alive):', e);
  }
};
```

<!-- Da MACH:INE II/src/config.js — aggiunta prevista dal piano 01 -->
```javascript
export const CFG = {
  V3_MODE: false,
  debug: false,
  // ... resto del CFG
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungi CFG.bpmLerpBeats a config.js (INFR-02)</name>
  <read_first>
    - Leggi `/Users/Edo_1/MACH-INE II/MACH:INE II/src/config.js` righe 6-30 per vedere la struttura attuale post-piano-01
  </read_first>
  <files>MACH:INE II/src/config.js</files>
  <action>
    In `MACH:INE II/src/config.js`, aggiungere `bpmLerpBeats: 2` subito dopo `V3_MODE: false` (prima riga del CFG aggiunta dal piano 01):

    ```javascript
    export const CFG = {
      V3_MODE: false,       // false = v2 behavior (7 motori); true = v3 layer system (Phase 1+)
      bpmLerpBeats: 2,      // BPM transition lerp duration in beats (tempo-relative smoothing)
      debug: false,
    ```

    SOLO questa aggiunta. `bpmLerpBeats: 2` significa: la transizione da un BPM al successivo si completa in 2 battute — un valore basso abbastanza da non sembrare strano, abbastanza alto da eliminare il click sul sync.
  </action>
  <acceptance_criteria>
    - `grep -n "bpmLerpBeats" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/config.js"` restituisce una riga con `bpmLerpBeats: 2`
    - La riga appare tra `V3_MODE` e `debug` nel file
  </acceptance_criteria>
  <verify>
    <automated>grep -n "bpmLerpBeats" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/config.js"</automated>
  </verify>
  <done>config.js ha `bpmLerpBeats: 2` dopo `V3_MODE: false`. Nessun'altra riga modificata.</done>
</task>

<task type="auto">
  <name>Task 2: Implementa lerp BPM in main.js (INFR-02)</name>
  <read_first>
    - Leggi `/Users/Edo_1/MACH-INE II/MACH:INE II/src/main.js` righe 196-252 per vedere lo stato attuale delle variabili e del handler
  </read_first>
  <files>MACH:INE II/src/main.js</files>
  <action>
    Tre modifiche chirurgiche in `main.js`:

    **Modifica A — Variabili module-level (dopo riga 199)**

    Trovare queste righe (circa riga 199-200):
    ```javascript
    let lastClockBpm = 120;
    let wasAnyComposerActive = false;
    ```

    Aggiungere DOPO `let lastClockBpm = 120;`:
    ```javascript
    let _targetClockBpm = 120;  // BPM target (updated instantly on engine switch)
    let _currentClockBpm = 120; // BPM actual (lerped toward _targetClockBpm each tick)
    ```

    **Modifica B — Funzione getActiveBpm()**

    Trovare la riga (circa 217):
    ```javascript
      lastClockBpm = e.bpm;
    ```

    Sostituire con:
    ```javascript
      lastClockBpm = e.bpm;
      _targetClockBpm = e.bpm; // set lerp target (guard: e.bpm is never null here)
    ```

    **Modifica C — midiWorker.onmessage handler**

    Trovare la riga (circa 245):
    ```javascript
        if (anyActive) updateMIDIClock(getActiveBpm());
    ```

    Sostituire con:
    ```javascript
        if (anyActive) {
          getActiveBpm(); // updates _targetClockBpm
          // Lerp _currentClockBpm toward _targetClockBpm over CFG.bpmLerpBeats beats
          const beatsPerTick = (dt / 1000) * (_currentClockBpm / 60);
          const lerpRate = beatsPerTick / CFG.bpmLerpBeats;
          _currentClockBpm += (_targetClockBpm - _currentClockBpm) * Math.min(lerpRate, 1);
          updateMIDIClock(_currentClockBpm);
        }
    ```

    SPIEGAZIONE della formula lerp:
    - `dt` è in millisecondi (dal Worker)
    - `beatsPerTick = (dt / 1000) * (bpm / 60)` — quante battute passano in un tick
    - `lerpRate = beatsPerTick / bpmLerpBeats` — frazione del lerp completata in questo tick
    - `Math.min(lerpRate, 1)` — clamp a 1 per evitare overshoot
    - Nessun NaN: `_targetClockBpm` non viene mai aggiornato a null (guard `e.bpm &&` in getActiveBpm), quindi `_currentClockBpm` resta al suo ultimo valore valido quando DERIVA è l'unico engine attivo

    GUARD DERIVA: La guardia `e.bpm &&` già presente in `getActiveBpm()` (riga 215 circa) scarta DERIVA (bpm: null). `_targetClockBpm` resta al suo ultimo valore valido. Non aggiungere guardie extra.
  </action>
  <acceptance_criteria>
    - `grep -n "_targetClockBpm" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/main.js"` restituisce almeno 2 righe (dichiarazione + assegnazione in getActiveBpm)
    - `grep -n "_currentClockBpm" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/main.js"` restituisce almeno 3 righe (dichiarazione + lerp + chiamata updateMIDIClock)
    - `grep -n "updateMIDIClock" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/main.js"` mostra `updateMIDIClock(_currentClockBpm)` (non più `getActiveBpm()` come argomento diretto)
    - Il file non contiene `updateMIDIClock(getActiveBpm())` (vecchio pattern eliminato)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "_targetClockBpm\|_currentClockBpm\|updateMIDIClock" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/main.js"</automated>
  </verify>
  <done>main.js ha le due variabili lerp dichiarate, `_targetClockBpm` viene settato in `getActiveBpm()`, il handler usa il lerp e passa `_currentClockBpm` a `updateMIDIClock`. Nessun'altra riga modificata.</done>
</task>

</tasks>

<verification>
1. `grep "bpmLerpBeats" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/config.js"` — output: `bpmLerpBeats: 2`
2. `grep "_targetClockBpm\|_currentClockBpm" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/main.js"` — output: almeno 4 righe
3. Apertura `http://localhost:8282` in Chrome — nessun errore console al boot
4. Verifica manuale: attivare engine TERRENO (80 BPM), poi switcher a MECCANICA (differente BPM) — MIDI monitor mostra intervallo F8 che cambia gradualmente, non di scatto
5. Verifica DERIVA: attivare solo DERIVA — clock continua a tick alla frequenza precedente senza NaN
</verification>

<success_criteria>
- `CFG.bpmLerpBeats: 2` presente in config.js
- `_targetClockBpm` e `_currentClockBpm` dichiarati in main.js
- Lerp applicato ogni tick Worker nel handler onmessage
- `updateMIDIClock(_currentClockBpm)` chiamato (non getActiveBpm come argomento diretto)
- Nessun NaN con DERIVA engine attivo
- Sul MIDI monitor, transizioni di engine mostrano F8 interval che cambia gradualmente su ~2 battute
</success_criteria>

<output>
Dopo il completamento, crea `.planning/phases/00-infrastruttura-e-migrazione/00-03-SUMMARY.md`
</output>
