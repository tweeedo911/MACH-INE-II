---
phase: 00-infrastruttura-e-migrazione
plan: 02
type: execute
wave: 2
depends_on:
  - "00-01"
files_modified:
  - "MACH:INE II/src/midi.js"
autonomous: true
requirements:
  - INFR-01
must_haves:
  truths:
    - "CLOCK_LOOKAHEAD è 100ms in midi.js (non 50ms)"
    - "Il MIDI clock non produce stutter misurabile durante un picco GC di 50ms sul main thread"
  artifacts:
    - path: "MACH:INE II/src/midi.js"
      provides: "CLOCK_LOOKAHEAD aumentato"
      contains: "CLOCK_LOOKAHEAD = 100"
  key_links:
    - from: "MACH:INE II/src/midi.js"
      to: "updateMIDIClock()"
      via: "CLOCK_LOOKAHEAD constant line 44"
      pattern: "CLOCK_LOOKAHEAD = 100"
---

<objective>
Aumenta il buffer lookahead del MIDI clock da 50ms a 100ms in midi.js.

Purpose: Il MIDI clock è schedulato con lookahead per isolare il timing dai picchi GC del main thread. Con 50ms di buffer, un major GC di V8 (fino a 50ms) può esaurire la finestra. 100ms fornisce 2× safety margin contro pause GC senza modifiche architetturali (WebMIDI non è trasferibile al Worker). Soddisfa INFR-01.
Output: Una riga modificata in midi.js: `CLOCK_LOOKAHEAD = 100`.
</objective>

<execution_context>
@/Users/Edo_1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Edo_1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Edo_1/MACH-INE II/.planning/ROADMAP.md
@/Users/Edo_1/MACH-INE II/.planning/phases/00-infrastruttura-e-migrazione/00-01-SUMMARY.md

<interfaces>
<!-- Da MACH:INE II/src/midi.js — riga 44 (attuale, pre-modifica) -->
```javascript
const CLOCK_LOOKAHEAD = 50; // schedule ticks up to 50ms ahead
```

<!-- Funzione target (righe 172-184 circa) -->
```javascript
export function updateMIDIClock(bpm) {
  // usa CLOCK_LOOKAHEAD per schedulare tick in anticipo via WebMIDI timestamp
}
```

<!-- Chiamata in main.js — NON toccare -->
// midiWorker.onmessage → updateMIDIClock(getActiveBpm())  — riga 245 main.js
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aumenta CLOCK_LOOKAHEAD a 100ms (INFR-01)</name>
  <read_first>
    - Leggi `/Users/Edo_1/MACH-INE II/MACH:INE II/src/midi.js` righe 37-50 per confermare la riga esatta della costante
  </read_first>
  <files>MACH:INE II/src/midi.js</files>
  <action>
    In `MACH:INE II/src/midi.js`, trovare la riga (attualmente riga 44):

    ```javascript
    const CLOCK_LOOKAHEAD = 50; // schedule ticks up to 50ms ahead
    ```

    Sostituire con:

    ```javascript
    const CLOCK_LOOKAHEAD = 100; // schedule ticks up to 100ms ahead — 2× margin against V8 major GC pauses (~50ms max)
    ```

    SOLO questa riga. Non toccare nessun'altra riga del file.

    NOTA: CFG non è usato per questa costante perché è interna a midi.js e non è un parametro compositivo. Il commento spiega il ragionamento (anti-pattern: no magic numbers — ma qui il commento basta perché la costante è locale a midi.js e documentata inline).

    Se in futuro si vuole esporre in CFG: `CFG.midiClockLookaheadMs: 100` — ma NON farlo ora (fuori scope).
  </action>
  <acceptance_criteria>
    - `grep -n "CLOCK_LOOKAHEAD" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/midi.js"` restituisce una riga contenente `= 100`
    - `grep -c "CLOCK_LOOKAHEAD" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/midi.js"` restituisce `2` (dichiarazione + uso in updateMIDIClock)
    - Il file non contiene più `CLOCK_LOOKAHEAD = 50`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "CLOCK_LOOKAHEAD" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/midi.js"</automated>
  </verify>
  <done>midi.js mostra `CLOCK_LOOKAHEAD = 100`. Nessun'altra riga modificata. Il file si carica senza errori ES module.</done>
</task>

</tasks>

<verification>
1. `grep "CLOCK_LOOKAHEAD" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/midi.js"` — output deve includere `= 100`
2. Apertura `http://localhost:8282` in Chrome — console DevTools non mostra errori JS
3. Verifica manuale con MIDI monitor: con un engine attivo, i tick F8 del clock arrivano a intervalli regolari senza gap visibili anche durante rendering pesante
</verification>

<success_criteria>
- Una sola riga modificata in midi.js
- `CLOCK_LOOKAHEAD` vale 100, non 50
- Nessun errore console al boot
- MIDI clock continuo senza stutter osservabile su MIDI monitor durante test
</success_criteria>

<output>
Dopo il completamento, crea `.planning/phases/00-infrastruttura-e-migrazione/00-02-SUMMARY.md`
</output>
