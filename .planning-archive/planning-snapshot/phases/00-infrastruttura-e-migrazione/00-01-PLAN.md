---
phase: 00-infrastruttura-e-migrazione
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "MACH:INE II/src/config.js"
autonomous: true
requirements:
  - MIGR-01
  - MIGR-02
must_haves:
  truths:
    - "Il branch `v2-archive` esiste nel repo locale e punta all'HEAD corrente di main"
    - "CFG.V3_MODE è la prima proprietà di CFG in config.js e vale false"
    - "Il codice v2 su `v2-archive` è identico a main prima di qualsiasi modifica"
  artifacts:
    - path: "MACH:INE II/src/config.js"
      provides: "V3_MODE flag"
      contains: "V3_MODE: false"
  key_links:
    - from: "git branch v2-archive"
      to: "current HEAD"
      via: "git branch command"
      pattern: "v2-archive"
    - from: "src/config.js"
      to: "CFG object"
      via: "first property"
      pattern: "V3_MODE: false"
---

<objective>
Archivia il codebase v2 su un branch git separato e aggiunge il flag V3_MODE a config.js.

Purpose: MIGR-01 garantisce che il lavoro v2 sia preservato intatto prima di qualsiasi modifica; MIGR-02 crea il flag booleano che tutte le fasi v3 useranno per attivare il nuovo sistema senza rompere il v2 esistente.
Output: Branch `v2-archive` locale, CFG.V3_MODE = false come prima proprietà di config.js.
</objective>

<execution_context>
@/Users/Edo_1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Edo_1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Edo_1/MACH-INE II/.planning/PROJECT.md
@/Users/Edo_1/MACH-INE II/.planning/ROADMAP.md
@/Users/Edo_1/MACH-INE II/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crea branch v2-archive (MIGR-01)</name>
  <read_first>
    - Run: `git -C "/Users/Edo_1/MACH-INE II" log --oneline -3` — conferma HEAD corrente
    - Run: `git -C "/Users/Edo_1/MACH-INE II" branch` — verifica che `v2-archive` NON esista già
  </read_first>
  <files>git branch only — nessun file modificato</files>
  <action>
    Esegui in sequenza:

    1. `git -C "/Users/Edo_1/MACH-INE II" branch v2-archive`
       Crea il branch che punta all'HEAD attuale di main. Non fare checkout — restare su main.

    2. Verifica: `git -C "/Users/Edo_1/MACH-INE II" log v2-archive --oneline -1`
       Output deve coincidere con `git log main --oneline -1`.

    NON fare `git push` — remote non garantito. NON fare `git checkout v2-archive`.
    Se il branch esiste già, verificare che punti all'HEAD corrente e procedere.
  </action>
  <verify>
    <automated>git -C "/Users/Edo_1/MACH-INE II" branch | grep v2-archive</automated>
  </verify>
  <done>`git branch` mostra `v2-archive`. `git log v2-archive --oneline -1` e `git log main --oneline -1` mostrano lo stesso commit hash.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Aggiungi CFG.V3_MODE a config.js (MIGR-02)</name>
  <read_first>
    - Leggi `/Users/Edo_1/MACH-INE II/MACH:INE II/src/config.js` righe 1-10 per confermare la struttura attuale del CFG object
  </read_first>
  <files>MACH:INE II/src/config.js</files>
  <action>
    In `MACH:INE II/src/config.js`, riga 7, la riga corrente è:

    ```javascript
    export const CFG = {
      debug: false,    // set true to enable periodic composer debug logs
    ```

    Inserire `V3_MODE: false,` come PRIMA proprietà dell'oggetto CFG, PRIMA di `debug`:

    ```javascript
    export const CFG = {
      V3_MODE: false,  // false = v2 behavior (7 motori); true = v3 layer system (Phase 1+)
      debug: false,    // set true to enable periodic composer debug logs
    ```

    SOLO questa modifica. Non toccare nessun'altra riga.
    Commento in inglese (code convention del progetto).
  </action>
  <acceptance_criteria>
    - `grep -n "V3_MODE" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/config.js"` restituisce una riga contenente `V3_MODE: false`
    - La riga con `V3_MODE` viene PRIMA della riga con `debug: false` nel file
    - `grep -c "V3_MODE" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/config.js"` restituisce `1` (una sola occorrenza)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "V3_MODE" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/config.js"</automated>
  </verify>
  <done>config.js mostra `V3_MODE: false` come prima proprietà di CFG. Nessun'altra riga modificata.</done>
</task>

</tasks>

<verification>
Dopo entrambi i task:
1. `git -C "/Users/Edo_1/MACH-INE II" branch | grep v2-archive` — output: `  v2-archive`
2. `git -C "/Users/Edo_1/MACH-INE II" diff v2-archive main -- "MACH:INE II/src/config.js"` — mostra solo l'aggiunta di V3_MODE
3. `grep "V3_MODE" "/Users/Edo_1/MACH-INE II/MACH:INE II/src/config.js"` — output: `  V3_MODE: false,`
</verification>

<success_criteria>
- `v2-archive` branch esiste e punta al commit pre-modifica
- `CFG.V3_MODE` è presente in config.js, è false, è la prima proprietà
- Nessun altro file modificato
- Il progetto si avvia normalmente su `python3 -m http.server 8282` (nessuna regressione ES module)
</success_criteria>

<output>
Dopo il completamento, crea `.planning/phases/00-infrastruttura-e-migrazione/00-01-SUMMARY.md`
</output>
