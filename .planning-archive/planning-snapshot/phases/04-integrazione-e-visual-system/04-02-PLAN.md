---
phase: 04-integrazione-e-visual-system
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - "MACH:INE II/src/director.js"
autonomous: true
requirements: [VISL-01, VISL-02]

must_haves:
  truths:
    - "Con V3_MODE attivo, ENGINE_PREFS v2 e' completamente bypassato — director usa LAYER_PREFS da CFG.VISUAL"
    - "Il layer dominante e' determinato dal max di macroState dimensions con isteresi"
    - "arcPercent guida le transizioni di scena in 5 atti — la arc state machine v2 e' bypassata"
    - "Le firme visive per layer sono distinte: harmony=cold+sparse, rhythm=amber+denso, melody=magenta+organico"
    - "engineRender riceve i valori dal layer dominante con lerp — nessun pop visivo"
    - "La arc-driven density clamping in V3_MODE usa i densityCap degli atti, non i ARC_PARAMS v2"
  artifacts:
    - path: "MACH:INE II/src/director.js"
      provides: "V3 gate con LAYER_PREFS, dominance logic, 5-act visual arc"
      contains: "_updateDirectorV3"
  key_links:
    - from: "MACH:INE II/src/director.js"
      to: "MACH:INE II/src/macro-composer.js"
      via: "import { macroState }"
      pattern: "macroState\\.arcPercent"
    - from: "MACH:INE II/src/director.js"
      to: "MACH:INE II/src/config.js"
      via: "CFG.VISUAL.layers"
      pattern: "CFG\\.VISUAL"
---

<objective>
Estendere director.js con il gate V3_MODE: importare macroState, determinare il layer dominante (D-01/D-03), applicare LAYER_PREFS da CFG.VISUAL con lerp (D-09), guidare transizioni scena/palette da arcPercent in 5 atti (D-04/D-06), bypassare ENGINE_PREFS e arc state machine v2 (D-02/D-05).

Purpose: Il director visivo v3 segue l'arco narrativo e distingue i layer compositivi — il deliverable visivo core della Phase 4.
Output: director.js esteso con _updateDirectorV3(), V3 gate in updateDirector(), V3 bypass per arc density clamping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-integrazione-e-visual-system/04-CONTEXT.md
@.planning/phases/04-integrazione-e-visual-system/04-RESEARCH.md

Leggere MACH:INE II/src/director.js INTERO prima di qualsiasi modifica.
Leggere MACH:INE II/src/macro-composer.js per confermare l'export macroState.
Leggere MACH:INE II/src/config.js per confermare la struttura CFG.VISUAL (creata dal plan 04-01).

FILE PROTETTI — NON MODIFICARE: render.js, field.js, colors.js

<interfaces>
<!-- macroState export da macro-composer.js (riga 10) -->
```javascript
export const macroState = {
  rhythmicDensity: 0,    // 0.0-1.0
  harmonicColor:   0,    // 0.0-1.0
  melodicActivity: 0,    // 0.0-1.0
  textureDepth:    0,    // 0.0-1.0
  currentMode:     'A_lydian',
  arcPercent:      0,    // 0.0-1.0 del concerto
  barClock:        0,
};
```

<!-- engineRender export da director.js (riga 510) — oggetto da popolare in V3 -->
```javascript
export const engineRender = {
  active: false,
  _engine: null,
  _lastMusicalPhase: null,
  dotSize: null,
  densityMul: null,
  midiScale: null,
  forceInvert: null,
  shapeScale: 1.0,
  trailMax: 64,
  densityGravity: 0,
  onsetWaveSpeed: null,
  flickerSpeed: null,
  midiDensityMul: 0.4,
  feedbackDecay: null,
};
```

<!-- CFG.VISUAL.layers (creato dal plan 04-01) -->
```javascript
CFG.VISUAL.layers.harmony  // { palette: 'cold', dotSize: 10, densityMul: 0.6, ... }
CFG.VISUAL.layers.rhythm   // { palette: 'amber', dotSize: 4, densityMul: 1.4, ... }
CFG.VISUAL.layers.melody   // { palette: 'magenta', dotSize: 7, densityMul: 0.9, ... }
CFG.VISUAL.layers.master   // { palette: 'bw', dotSize: 6, densityMul: 0.3, ... }

CFG.VISUAL.acts.I   // { min: 0.00, max: 0.15, scene: 'SPARSE', palette: 'cold', densityCap: 0.15, camera: 'WIDE' }
CFG.VISUAL.acts.II  // ...
CFG.VISUAL.acts.III // ...
CFG.VISUAL.acts.IV  // ...
CFG.VISUAL.acts.V   // ...

CFG.VISUAL.dominanceThreshold   // 0.05
CFG.VISUAL.dominanceHysteresis  // 0.10
CFG.VISUAL.lerpSpeed            // 0.02
CFG.VISUAL.paletteLerpSpeed     // 0.015
```

<!-- API disponibili gia importate in director.js -->
```javascript
import { setPalette } from './colors.js';          // setPalette('cold') — gia usa lerp interno
import { setFraming } from './... (interno)';       // setFraming('WIDE', W, H) — gia disponibile
// transitionToScene(sceneObj, instant) — funzione interna director.js
// SCENES array — 8 scene objects con .name property
```

<!-- Struttura di director.js — punti di inserimento -->
Riga 1-13:    Imports                    <-- AGGIUNGERE import macroState qui
Riga 97-171:  ENGINE_PREFS (v2)          <-- DOPO questo: aggiungere _v3State e _updateDirectorV3
Riga 322-453: ARC_PARAMS + updateArc()   <-- NON toccare, sara bypassato in V3_MODE
Riga 500-525: engineRender               <-- Oggetto target per V3 override
Riga 805-918: updateDirector()           <-- INSERIRE V3 gate PRIMA di "const curEngine = getEngine()"
Riga 960-975: Arc-driven density clamping <-- INSERIRE V3 bypass prima del clamping v2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 04-02-01: Import macroState e stato interno V3</name>
  <files>MACH:INE II/src/director.js</files>
  <action>
Leggere director.js INTERO.

**Edit 1 — Import macroState (riga ~13, dopo l'ultimo import):**

Aggiungere dopo `import { getEnginePhase } from './presence-multiplier.js';`:

```javascript
import { macroState } from './macro-composer.js';
```

**Edit 2 — Stato interno V3 (dopo ENGINE_PREFS, ~riga 172, prima del commento ENGINE_VISUAL_PHASES):**

Aggiungere:

```javascript
// ═══════════════════════════════════════════════════════════
//  V3 VISUAL SYSTEM — layer dominance + arc visivo (Phase 4)
// ═══════════════════════════════════════════════════════════

let _v3DominantLayer = null;   // 'harmony' | 'rhythm' | 'melody' | null (= master)
let _v3LastAct       = null;   // 'I' | 'II' | ... | 'V' — per guard transitionToScene
let _v3LerpState     = {       // stato lerp corrente per engineRender
  dotSize: 6, densityMul: 0.3, midiScale: 1.0,
  shapeScale: 1.0, trailMax: 64, densityGravity: 0,
  onsetWaveSpeed: 400, flickerSpeed: 1.0, midiDensityMul: 0.3, feedbackDecay: 0.97,
};
```

Invarianti:
- NON rimuovere o modificare gli import esistenti
- NON modificare ENGINE_PREFS
  </action>
  <verify>
    <automated>grep -q "import { macroState } from './macro-composer.js'" "MACH:INE II/src/director.js" && grep -q "_v3DominantLayer" "MACH:INE II/src/director.js" && grep -q "_v3LastAct" "MACH:INE II/src/director.js" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>macroState importato, stato V3 interno dichiarato. Nessuna modifica funzionale — solo setup per i task successivi.</done>
</task>

<task type="auto">
  <name>Task 04-02-02: Funzione _updateDirectorV3 e V3 gate in updateDirector</name>
  <files>MACH:INE II/src/director.js</files>
  <action>
**Edit 3 — Funzione _updateDirectorV3 (dopo il blocco _v3LerpState, prima di ENGINE_VISUAL_PHASES):**

Aggiungere la funzione helper e il core V3:

```javascript
/**
 * Determina quale atto narrativo corrisponde a arcPercent (D-06).
 * Ritorna la chiave dell'atto ('I', 'II', ..., 'V').
 */
function _getV3Act(arcPct) {
  const acts = CFG.VISUAL.acts;
  if (arcPct < acts.II.min)  return 'I';
  if (arcPct < acts.III.min) return 'II';
  if (arcPct < acts.IV.min)  return 'III';
  if (arcPct < acts.V.min)   return 'IV';
  return 'V';
}

/**
 * Determina il layer dominante con isteresi (D-01, D-03).
 * Ritorna 'harmony' | 'rhythm' | 'melody' | null (master).
 */
function _getDominantLayer() {
  const m = macroState;
  const vals = {
    rhythm:  m.rhythmicDensity,
    harmony: m.harmonicColor,
    melody:  Math.max(m.melodicActivity, m.textureDepth),
  };

  const threshold = CFG.VISUAL.dominanceThreshold;
  const hysteresis = CFG.VISUAL.dominanceHysteresis;

  let best = null, bestVal = threshold;
  for (const [k, v] of Object.entries(vals)) {
    if (v > bestVal) { best = k; bestVal = v; }
  }

  // Isteresi: il corrente rimane dominante finche un altro lo supera di hysteresis
  if (_v3DominantLayer && best !== _v3DominantLayer) {
    const currentVal = vals[_v3DominantLayer] || 0;
    if (bestVal - currentVal < hysteresis) {
      return _v3DominantLayer;  // corrente tiene
    }
  }

  return best;
}

/**
 * Core V3 director update — chiamato in V3_MODE al posto di ENGINE_PREFS block.
 * Applica: layer dominance, LAYER_PREFS lerp su engineRender, 5-act scene/palette/camera.
 */
function _updateDirectorV3(dt) {
  const vis = CFG.VISUAL;
  const arcPct = macroState.arcPercent;

  // ── 1. Layer dominante (D-01, D-03) ──
  const dominant = _getDominantLayer();
  _v3DominantLayer = dominant;
  const layerKey = dominant || 'master';
  const prefs = vis.layers[layerKey];

  // ── 2. Lerp engineRender verso LAYER_PREFS del dominante (D-09) ──
  const lr = vis.lerpSpeed;
  const ls = _v3LerpState;

  ls.dotSize        += (prefs.dotSize        - ls.dotSize)        * lr;
  ls.densityMul     += (prefs.densityMul     - ls.densityMul)     * lr;
  ls.midiScale      += (prefs.midiScale      - ls.midiScale)      * lr;
  ls.shapeScale     += (prefs.shapeScale     - ls.shapeScale)     * lr;
  ls.trailMax       += (prefs.trailMax       - ls.trailMax)       * lr;
  ls.densityGravity += (prefs.densityGravity - ls.densityGravity) * lr;
  ls.onsetWaveSpeed += (prefs.onsetWaveSpeed - ls.onsetWaveSpeed) * lr;
  ls.flickerSpeed   += (prefs.flickerSpeed   - ls.flickerSpeed)   * lr;
  ls.midiDensityMul += (prefs.midiDensityMul - ls.midiDensityMul) * lr;
  ls.feedbackDecay  += (prefs.feedbackDecay  - ls.feedbackDecay)  * lr;

  // Scrive su engineRender (letto da field.js e render.js)
  engineRender.active       = true;
  engineRender._engine      = 'v3_' + layerKey;
  engineRender.dotSize      = ls.dotSize;
  engineRender.densityMul   = ls.densityMul;
  engineRender.midiScale    = ls.midiScale;
  engineRender.forceInvert  = null;
  engineRender.shapeScale   = ls.shapeScale;
  engineRender.trailMax     = Math.round(ls.trailMax);
  engineRender.densityGravity = ls.densityGravity;
  engineRender.onsetWaveSpeed = Math.round(ls.onsetWaveSpeed);
  engineRender.flickerSpeed = ls.flickerSpeed;
  engineRender.midiDensityMul = ls.midiDensityMul;
  engineRender.feedbackDecay = ls.feedbackDecay;

  // ── 3. Arco visivo 5 atti (D-04, D-06) ──
  const act = _getV3Act(arcPct);
  const actCfg = vis.acts[act];

  // Cambio atto → transizione scena + palette + camera
  if (act !== _v3LastAct) {
    _v3LastAct = act;
    // Trova scena target per nome
    const targetScene = SCENES.find(s => s.name === actCfg.scene);
    if (targetScene) transitionToScene(targetScene, false);
    // Palette: usa la palette dell'atto come base, sovrascritta dal layer dominante
    setPalette(prefs.palette);
    // Camera: richiedi framing atto
    if (actCfg.camera) requestFraming(actCfg.camera);
  }

  // Palette continua: se il layer dominante cambia fuori dai confini di atto,
  // aggiorna la palette gradualmente (il lerp interno di setPalette fa il resto)
  if (engineRender._lastMusicalPhase !== layerKey) {
    engineRender._lastMusicalPhase = layerKey;
    setPalette(prefs.palette);
  }
}
```

**Edit 4 — V3 gate in updateDirector() (riga ~808, DENTRO updateDirector, PRIMA di `const curEngine = getEngine()`):**

Wrappare il blocco ENGINE_PREFS esistente in un if/else:

```javascript
  // ── V3_MODE gate (Phase 4) ──
  if (CFG.V3_MODE) {
    _updateDirectorV3(dt);
  } else {
    // blocco ENGINE_PREFS v2 esistente — tutto invariato
    const curEngine = getEngine();
    // ... (tutto il codice esistente fino alla chiusura del blocco else di curPrefs, riga ~918)
  }
```

ATTENZIONE: Il codice da riga 808 a riga ~918 (blocco `const curEngine = getEngine()` fino alla fine dell'else che resetta engineRender) va DENTRO il ramo `else`. NON spostare, NON riscrivere — solo aggiungere `if (CFG.V3_MODE) { _updateDirectorV3(dt); } else {` prima e `}` dopo.

Le righe successive (da `director.sceneTime += dt;` in poi: updateArc, updateSceneBlend, dynamic compositions, beat accumulator, camera) restano FUORI dall'if/else — continuano ad eseguirsi in entrambe le modalita.

**Edit 5 — V3 bypass per arc-driven density clamping (riga ~960, DENTRO updateDirector):**

Trovare il blocco che inizia con `// Arc-driven density clamping` (circa riga 963). PRIMA di quel blocco aggiungere:

```javascript
  // V3_MODE: density clamping guidato da arcPercent, non da arc state machine (D-05)
  if (CFG.V3_MODE) {
    const v3Act = _getV3Act(macroState.arcPercent);
    const v3Cap = CFG.VISUAL.acts[v3Act].densityCap;
    scene.densityMul = Math.min(scene.densityMul, v3Cap);
  } else {
```

E DOPO il blocco density clamping v2 esistente (che termina prima di `const bpm = audio.bpm;`), chiudere l'else:

```javascript
  } // fine else V3_MODE density clamping
```

Invarianti:
- NON modificare il codice interno del blocco ENGINE_PREFS v2 — solo wrapparlo
- NON modificare updateArc() — viene comunque chiamata ma i suoi effetti su scene.densityMul sono sovrascrive dal V3 gate
- NON modificare render.js, field.js, colors.js (PROTECTED)
- NON creare nuove export — tutto resta interno a director.js (D-01)
- Il beat accumulator (riga ~978) e camera lerp (riga ~1050) DEVONO continuare a funzionare in V3_MODE — non metterli dentro l'if/else
  </action>
  <verify>
    <automated>grep -q "_updateDirectorV3" "MACH:INE II/src/director.js" && grep -q "_getDominantLayer" "MACH:INE II/src/director.js" && grep -q "_getV3Act" "MACH:INE II/src/director.js" && grep -q "import { macroState }" "MACH:INE II/src/director.js" && grep -q "CFG.V3_MODE" "MACH:INE II/src/director.js" && grep -q "CFG.VISUAL" "MACH:INE II/src/director.js" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
Con V3_MODE attivo:
- ENGINE_PREFS v2 completamente bypassato (D-02)
- Layer dominante determinato da macroState con isteresi (D-01, D-03)
- engineRender riceve parametri dal LAYER_PREFS del dominante via lerp (D-09)
- Transizioni scena/palette/camera guidate da arcPercent in 5 atti (D-04, D-06)
- arc state machine v2 bypassata per density clamping (D-05)
- Nessun pop visivo — tutti i parametri lerpati
  </done>
</task>

</tasks>

<verification>
- Avviare server: `cd "MACH:INE II" && python3 -m http.server 8282`
- Aprire `http://localhost:8282` su Chrome, cliccare per avviare
- Con CFG.V3_MODE = true (default), il campo visivo deve mostrare la palette del layer dominante
- In console: `macroState.arcPercent = 0.0` → campo sparse/freddo (atto I)
- In console: `macroState.arcPercent = 0.5` → campo denso/caldo (atto III)
- Nessun errore JS in console
</verification>

<success_criteria>
- director.js importa macroState da macro-composer.js
- Il blocco ENGINE_PREFS v2 e' wrappato in `if (!CFG.V3_MODE)` — codice v2 intatto
- _updateDirectorV3 applica LAYER_PREFS da CFG.VISUAL con lerp su engineRender
- 5 atti narrativi guidati da arcPercent con transizioni scena/palette/camera
- Density clamping v3 usa densityCap dagli atti, non da ARC_PARAMS v2
- Zero nuove export — tutto interno (D-01)
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/04-integrazione-e-visual-system/04-02-SUMMARY.md`
</output>
