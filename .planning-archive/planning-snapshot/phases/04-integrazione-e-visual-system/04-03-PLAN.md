---
phase: 04-integrazione-e-visual-system
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - "MACH:INE II/src/main.js"
autonomous: false
requirements: [MARC-04, VISL-01, VISL-02]

must_haves:
  truths:
    - "window._m e' disponibile in console per override arcPercent durante test"
    - "5 checkpoint a arcPercent 0/25/50/75/90 passano senza errori JS"
    - "Ad ogni checkpoint almeno un layer emette note MIDI (nessun silenzio totale fuori dissoluzione)"
    - "Il campo visivo riflette l'atto corretto: sparse/freddo a 0%, denso/caldo a 50%, dissoluzione a 90%"
    - "Il cambio layer dominante produce firma visiva distinta (palette diversa percepibile)"
  artifacts:
    - path: "MACH:INE II/src/main.js"
      provides: "Debug helper window._m per test MARC-04"
      contains: "window._m"
  key_links:
    - from: "MACH:INE II/src/main.js"
      to: "MACH:INE II/src/macro-composer.js"
      via: "window._m = macroState"
      pattern: "window\\._m"
---

<objective>
Aggiungere debug helper `window._m` per override arcPercent da console ed eseguire la checklist di integrazione MARC-04: 5 checkpoint a arcPercent forzato (0%, 25%, 50%, 75%, 90%) verificando stabilita compositiva, arco visivo e assenza errori.

Purpose: Verificare che la suite 45-60 minuti regga senza collasso compositivo (MARC-04) e che visual system + firme layer funzionino end-to-end (VISL-01/02).
Output: Debug helper in main.js + checklist verificata + checkpoint umano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-integrazione-e-visual-system/04-CONTEXT.md
@.planning/phases/04-integrazione-e-visual-system/04-RESEARCH.md

Leggere MACH:INE II/src/main.js per trovare il punto di inserimento debug helper.

<interfaces>
<!-- macroState da macro-composer.js — gia importato in main.js indirettamente -->
<!-- main.js importa initMacroComposer da macro-composer.js -->
<!-- Per window._m serve import esplicito di macroState -->

Da MACH:INE II/src/main.js (riga ~146-155):
```javascript
if (CFG.V3_MODE) {
    initMacroComposer();
    initHarmonyLayer();
    initRhythmLayer();
    initMelodyTextureLayer();
    sendMIDIStart();
    console.log('[V3] MacroComposer + HarmonyLayer + RhythmLayer + MelodyTextureLayer initialized');
}
```

Da MACH:INE II/src/macro-composer.js:
```javascript
export const macroState = { rhythmicDensity, harmonicColor, melodicActivity, textureDepth, arcPercent, ... };
```

Segnali di collasso MARC-04 (D-11):
1. Silenzio MIDI totale >30s fuori dissoluzione (arcPercent > 0.85)
2. Note out-of-range per canale
3. Campo visivo fermo o non risponde al MIDI
4. Monotonia armonica >10min nella sezione centrale

Procedura checkpoint (D-10):
- Fermare sequencer (Shift+0)
- Forzare macroState.arcPercent da console
- Attendere 10-15 secondi per stabilizzazione
- Verificare MIDI output + visual + console
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 04-03-01: Aggiungere debug helper window._m in main.js</name>
  <files>MACH:INE II/src/main.js</files>
  <action>
Leggere main.js INTERO.

**Edit 1 — Import macroState (dopo gli import v3 esistenti, circa riga 5-10):**

Trovare la riga che importa da macro-composer.js (es. `import { initMacroComposer, updateMacroComposer } from './macro-composer.js';`). Aggiungere `macroState` alla lista degli import:

```javascript
import { initMacroComposer, updateMacroComposer, macroState } from './macro-composer.js';
```

Se macroState e' gia importato, non duplicare.

**Edit 2 — Debug helper (dentro il blocco `if (CFG.V3_MODE)`, dopo il console.log, circa riga 153):**

Aggiungere dopo `console.log('[V3] MacroComposer + ...');`:

```javascript
    // Debug helper per checkpoint MARC-04 (D-10) — override arcPercent da console
    // Uso: window._m.arcPercent = 0.5 (con sequencer fermo: Shift+0)
    if (CFG.debug) {
      window._m = macroState;
      console.log('[V3] Debug: window._m disponibile — usa window._m.arcPercent = N per test');
    }
```

**Edit 3 — Abilitare debug mode (OBBLIGATORIO per checkpoint MARC-04):**

`window._m` e' esposto solo quando `CFG.debug === true`. Senza questo edit, il checkpoint 04-03-02 non puo essere eseguito (window._m sara undefined in console).

Modificare config.js — trovare la riga con `debug:` e impostare:

```javascript
  debug: true,  // TEMPORANEO: abilitato per checkpoint MARC-04 — rimettere false dopo i test
```

NOTA: dopo il completamento di tutti i checkpoint, rimettere `debug: false`.

Invarianti:
- NON aggiungere window._m al di fuori del blocco `if (CFG.debug)` — mai esposto in produzione
- NON modificare la logica V3_MODE esistente (init, update nel Worker)
- NON modificare render.js, field.js, colors.js (PROTECTED)
  </action>
  <verify>
    <automated>grep -q "window._m" "MACH:INE II/src/main.js" && grep -q "macroState" "MACH:INE II/src/main.js" && grep -q "debug: true" "MACH:INE II/src/config.js" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>window._m disponibile in console (CFG.debug=true confermato in config.js). macroState importato in main.js. Checkpoint MARC-04 eseguibile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-03-02: Checklist integrazione MARC-04 — 5 checkpoint arcPercent</name>
  <what-built>
Visual system v3 completo: CFG.VISUAL config, V3 gate in director.js (LAYER_PREFS + dominance + 5-act arc), debug helper window._m.
  </what-built>
  <how-to-verify>
**Preparazione:**
1. Avviare server: `cd "MACH:INE II" && python3 -m http.server 8282`
2. Aprire `http://localhost:8282` su Chrome
3. Cliccare per avviare — verificare che in console appaia `[V3] Debug: window._m disponibile`
4. Fermare il sequencer: premere `Shift+0`

**Checkpoint 1 — arcPercent 0% (Atto I — Emergenza):**
- In console: `window._m.arcPercent = 0.0`
- Attendere 10 secondi
- VERIFICARE:
  - [ ] Campo visivo sparse, rado, palette fredda (cold/blu)
  - [ ] Nessun errore JS in console
  - [ ] MIDI monitor: almeno un canale emette note (harmony layer drone attivo)

**Checkpoint 2 — arcPercent 25% (Atto II — Discesa):**
- In console: `window._m.arcPercent = 0.25`
- Attendere 10 secondi
- VERIFICARE:
  - [ ] Densita crescente rispetto a checkpoint 1
  - [ ] Palette transitata verso neutro/default
  - [ ] Nessun errore JS in console
  - [ ] MIDI: piu canali attivi rispetto a checkpoint 1

**Checkpoint 3 — arcPercent 50% (Atto III — Macchina):**
- In console: `window._m.arcPercent = 0.50`
- Attendere 10 secondi
- VERIFICARE:
  - [ ] Campo denso, palette calda (warm/amber)
  - [ ] Firma layer: se rhythm domina, palette ambra. Se harmony, palette fredda. Distinguibili.
  - [ ] Nessun errore JS in console
  - [ ] MIDI: tutti i canali attivi, densita massima

**Checkpoint 4 — arcPercent 75% (Atto IV — Intensita):**
- In console: `window._m.arcPercent = 0.75`
- Attendere 10 secondi
- VERIFICARE:
  - [ ] Campo ancora denso ma con prime "smagliature"
  - [ ] Nessun errore JS in console
  - [ ] MIDI: attivo, nessuna monotonia

**Checkpoint 5 — arcPercent 90% (Atto V — Dissoluzione):**
- In console: `window._m.arcPercent = 0.90`
- Attendere 10 secondi
- VERIFICARE:
  - [ ] Campo che si svuota, palette fredda che ritorna
  - [ ] Nessun errore JS in console
  - [ ] MIDI: puo essere rarefatto, il silenzio e' previsto in dissoluzione

**Test firma layer (VISL-02):**
- In console: `window._m.rhythmicDensity = 1.0; window._m.harmonicColor = 0; window._m.melodicActivity = 0`
- VERIFICARE: palette ambra (rhythm domina)
- In console: `window._m.rhythmicDensity = 0; window._m.harmonicColor = 1.0; window._m.melodicActivity = 0`
- VERIFICARE: palette fredda (harmony domina)
- In console: `window._m.rhythmicDensity = 0; window._m.harmonicColor = 0; window._m.melodicActivity = 1.0`
- VERIFICARE: palette magenta (melody domina)

**Segnali di collasso (D-11) — NESSUNO deve verificarsi:**
- [ ] Nessun silenzio MIDI totale >30s (fuori da checkpoint 5)
- [ ] Nessuna nota out-of-range (CH1 C2-C4, CH2 C3-C5, CH3 C1-C3, CH4 C4-C7)
- [ ] Campo visivo non fermo — risponde al MIDI
- [ ] Nessuna monotonia armonica (currentMode cambia nel tempo)
  </how-to-verify>
  <resume-signal>Digita "approved" se tutti i 5 checkpoint passano e le firme layer sono distinguibili. Altrimenti descrivi i problemi.</resume-signal>
</task>

</tasks>

<verification>
Tutti i 5 checkpoint passano senza errori JS. Il campo visivo riflette l'atto corretto per ogni arcPercent. Le firme layer sono percettivamente distinte (palette diversa per harmony/rhythm/melody). Nessun segnale di collasso.
</verification>

<success_criteria>
- MARC-04: La suite regge ai 5 checkpoint forzati senza collasso compositivo, errori JS o silenzio MIDI imprevisto
- VISL-01: L'arco visivo e' riconoscibile — sparse/freddo a 0%, denso/caldo a 50%, dissoluzione a 90%
- VISL-02: Le firme per layer sono distinguibili — palette chiaramente diversa per harmony (cold), rhythm (amber), melody (magenta)
- window._m e' disponibile per future sessioni di debug
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/04-integrazione-e-visual-system/04-03-SUMMARY.md`
</output>
