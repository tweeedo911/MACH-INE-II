---
phase: 04-integrazione-e-visual-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "MACH:INE II/src/config.js"
autonomous: true
requirements: [VISL-01, VISL-02]

must_haves:
  truths:
    - "CFG.VISUAL esiste in config.js con tutti i parametri per LAYER_PREFS, arco visivo 5 atti e dominance"
    - "Le palette per layer sono definite: harmony=cold, rhythm=amber, melody=magenta, master=bw"
    - "I 5 atti narrativi visivi hanno soglie arcPercent coerenti con D-06"
    - "dominanceHysteresis e lerpSpeed sono parametri configurabili, non magic numbers"
  artifacts:
    - path: "MACH:INE II/src/config.js"
      provides: "CFG.VISUAL config block"
      contains: "VISUAL:"
  key_links:
    - from: "MACH:INE II/src/config.js"
      to: "MACH:INE II/src/director.js"
      via: "import { CFG } from './config.js'"
      pattern: "CFG\\.VISUAL"
---

<objective>
Aggiungere il blocco `CFG.VISUAL` a config.js con tutti i parametri numerici per il visual system v3: LAYER_PREFS per layer (palette, dotSize, densityMul, midiScale, trailMax, flickerSpeed), soglie arco visivo 5 atti, parametri di dominance e lerp.

Purpose: Centralizzare tutti i valori visivi v3 in config.js prima di implementare il gate V3 in director.js (config-first pattern stabilito in tutte le fasi precedenti).
Output: Sezione CFG.VISUAL in config.js — zero magic numbers pronti per director.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-integrazione-e-visual-system/04-CONTEXT.md
@.planning/phases/04-integrazione-e-visual-system/04-RESEARCH.md

Leggere MACH:INE II/src/config.js per trovare il punto di inserimento (dopo la chiusura di CFG.MELODY, prima di `};` finale).

<interfaces>
<!-- Struttura CFG esistente — pattern da seguire -->
<!-- Ogni blocco layer e' un sotto-oggetto di CFG: CFG.MACRO, CFG.RHYTHM, CFG.MELODY -->
<!-- Il nuovo blocco CFG.VISUAL segue lo stesso pattern -->

Da MACH:INE II/src/config.js (righe 686-693):
```javascript
    // fine di CFG.MELODY...
    },
  },
};
```

Da MACH:INE II/src/director.js — ENGINE_PREFS (righe 98-171):
```javascript
// Ogni engine v2 ha: palette, dotSize, densityMul, midiScale, forceInvert,
//   shapeScale, trailMax, densityGravity, onsetWaveSpeed, flickerSpeed, midiDensityMul, feedbackDecay
// LAYER_PREFS v3 replica la stessa struttura per 3 layer + master
```

Palette disponibili in colors.js:
'default', 'amber', 'cyan', 'bw', 'magenta', 'warm', 'cold', 'ice', 'abyssal', 'steel', 'ikeda'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 04-01-01: Aggiungere CFG.VISUAL config block</name>
  <files>MACH:INE II/src/config.js</files>
  <action>
Leggere config.js intero. Trovare la chiusura di CFG.MELODY (riga ~689: `  },`) seguita da `};` che chiude l'intero CFG object.

Inserire il blocco `VISUAL` DOPO la chiusura di MELODY e PRIMA di `};` finale. Usare lo stesso stile dei blocchi precedenti (MACRO, RHYTHM, MELODY).

Contenuto del blocco CFG.VISUAL:

```javascript
  // ── Visual System v3 (Phase 4) ─────────────────────────────────────────────
  VISUAL: {
    // ── Dominance logic (D-01, D-03) ──
    dominanceThreshold: 0.05,   // valore minimo per dichiarare un layer dominante
    dominanceHysteresis: 0.10,  // margine che il nuovo layer deve superare per detronizzare il corrente
    lerpSpeed: 0.02,            // lerp rate per parametri render tra layer (per frame a 60fps)
    paletteLerpSpeed: 0.015,    // lerp rate cambio palette (piu lento per evitare tremolio)

    // ── 5 atti narrativi visivi (D-06) ──
    // Ogni atto: range arcPercent, scena target, palette base, densityCap, camera
    acts: {
      I:   { min: 0.00, max: 0.15, scene: 'SPARSE',         palette: 'cold',    densityCap: 0.15, camera: 'WIDE'   },
      II:  { min: 0.15, max: 0.35, scene: 'BAYER_CLASSIC',   palette: 'default', densityCap: 0.45, camera: 'DRIFT'  },
      III: { min: 0.35, max: 0.60, scene: 'COLORED_GROUND',  palette: 'warm',    densityCap: 1.00, camera: 'MEDIUM' },
      IV:  { min: 0.60, max: 0.80, scene: 'DENSE',           palette: 'warm',    densityCap: 0.90, camera: 'MEDIUM' },
      V:   { min: 0.80, max: 1.00, scene: 'SPARSE',          palette: 'cold',    densityCap: 0.12, camera: 'WIDE'   },
    },

    // ── Layer preferences — firma visiva per layer (D-07, D-08, D-09) ──
    // Analogo a ENGINE_PREFS v2 ma per i 3 layer v3 + master
    layers: {
      harmony: {
        palette: 'cold',
        dotSize: 10,  densityMul: 0.6,  midiScale: 2.0,
        trailMax: 48, flickerSpeed: 0.5,
        shapeScale: 0.4, densityGravity: -0.2,
        onsetWaveSpeed: 300, midiDensityMul: 0.5, feedbackDecay: 0.96,
      },
      rhythm: {
        palette: 'amber',
        dotSize: 4,   densityMul: 1.4,  midiScale: 1.0,
        trailMax: 32, flickerSpeed: 5.0,
        shapeScale: 1.2, densityGravity: 0.15,
        onsetWaveSpeed: 1000, midiDensityMul: 0.4, feedbackDecay: 0.93,
      },
      melody: {
        palette: 'magenta',
        dotSize: 7,   densityMul: 0.9,  midiScale: 1.5,
        trailMax: 40, flickerSpeed: 2.0,
        shapeScale: 1.0, densityGravity: 0,
        onsetWaveSpeed: 600, midiDensityMul: 0.6, feedbackDecay: 0.95,
      },
      // Master — quando nessun layer domina (macroState tutto basso)
      master: {
        palette: 'bw',
        dotSize: 6,   densityMul: 0.3,  midiScale: 1.0,
        trailMax: 64, flickerSpeed: 1.0,
        shapeScale: 1.0, densityGravity: 0,
        onsetWaveSpeed: 400, midiDensityMul: 0.3, feedbackDecay: 0.97,
      },
    },
  },
```

Invarianti:
- NON modificare nessun altro blocco CFG (MACRO, RHYTHM, MELODY)
- Usare lo stesso stile commento (// ── Sezione ──) dei blocchi precedenti
- Tutte le palette referenziate DEVONO esistere in colors.js PALETTES
- I nomi scene referenziati (SPARSE, BAYER_CLASSIC, COLORED_GROUND, DENSE) DEVONO corrispondere ai nomi in SCENES[] di director.js
  </action>
  <verify>
    <automated>grep -q "VISUAL:" "MACH:INE II/src/config.js" && grep -q "dominanceHysteresis" "MACH:INE II/src/config.js" && grep -q "harmony:" "MACH:INE II/src/config.js" && grep -q "rhythm:" "MACH:INE II/src/config.js" && grep -q "melody:" "MACH:INE II/src/config.js" && grep -q "master:" "MACH:INE II/src/config.js" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CFG.VISUAL esiste con: 4 layer preferences (harmony/rhythm/melody/master), 5 atti visivi con soglie arcPercent, parametri dominance/lerp. Zero magic numbers — tutti i valori calibrabili.</done>
</task>

</tasks>

<verification>
- `grep -c "VISUAL:" "MACH:INE II/src/config.js"` ritorna 1
- `node -e "import('./MACH:INE II/src/config.js').then(m => { const v = m.CFG.VISUAL; console.log(Object.keys(v.layers).length === 4 ? 'OK' : 'FAIL'); })"` — verifica che il file sia importabile come ES module
- Le 4 palette (cold, amber, magenta, bw) corrispondono alle palette in colors.js
</verification>

<success_criteria>
- CFG.VISUAL.layers ha 4 sotto-oggetti: harmony, rhythm, melody, master (per D-08, D-09)
- CFG.VISUAL.acts ha 5 atti con range arcPercent coerenti (D-06)
- CFG.VISUAL.dominanceHysteresis e lerpSpeed sono definiti (D-03)
- config.js e' valido ES module — nessun errore di sintassi
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/04-integrazione-e-visual-system/04-01-SUMMARY.md`
</output>
